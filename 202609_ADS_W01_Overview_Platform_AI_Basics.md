# 第1周 课程概述、学习平台与 AI 基础

*Updated 2026-09-15 GMT+8*
 *Compiled by Hongfei Yan (2026 Fall)*
https://github.com/GMyhf/2026fall-cs101

> **课程安排对应**：第 1 周
> **主题与学习重点**：课程概述、学习平台与 AI 基础；完成课前准备与盲打练习。

**知识点**：课程目标与节奏、成绩构成、OpenJudge / LeetCode / 小北智学三个平台、开发环境选型、盲打与课前准备、AI 与大语言模型的基本概念、AI 助教的正确用法与学术诚信边界、第一个 Python 程序、`input()` / `print()` 的基本用法。

> 课程材料
>
> ![image-20260912112323097](https://raw.githubusercontent.com/GMyhf/img1/main/image-20260912112323097.png)

---

# 1 课程概述

## 1.1 这门课教什么

《计算概论（B）》课程编号 04831410，学分 3，**48 学时理论课（每周 3 学时）+ 32 学时上机实习课（每周 2 学时）**。

课程以**编程问题求解**为主线，培养两种思维：

| 思维 | 含义 | 本课的落点 |
| ---- | ---- | ---- |
| 数学思维 | 把现实问题抽象成可计算的模型 | 递推关系、状态定义、复杂度估算 |
| 计算机思维 | 在有限资源下设计可执行的步骤 | 数据结构选型、边界处理、调试 |

完成课程后，你应当能够：

1. 用 Python 完成基本的输入输出、控制结构、函数与常用数据结构操作。对基础 C++ 程序的阅读能力为不作硬性要求。；
2. 运用复杂度分析、矩阵、排序、贪心、递归、回溯、动态规划和图搜索等方法分析并解决问题；
3. 理解图灵机、进程虚拟地址空间和 ASCII 编码等计算机基本原理；
4. 在 OpenJudge 等在线评测平台独立调试程序、构造测试数据并改进解法。

> 课堂以 **Python** 为主要讲解语言，也支持使用 **C++** 完成练习。

## 1.2 十六周的节奏

| 阶段 | 周次 | 关键词 |
| ---- | ---- | ---- |
| 打基础 | 1–4 | 环境、Shell、计算机原理（1/2）、Python 语法与算法分析 |
| 上强度 | 5–9 | 10 月月考、矩阵 / 排序 / 贪心、栈与队列、递归、回溯、并查集 |
| 攻核心 | 10–12 | 区间问题、动态规划、BFS |
| 融会贯通 | 13–16 | 计算机原理（2/2）、AI 素养、知识图谱与神经网络、总复习与上机考试 |

一句话版本：**9 月打基础，10 月上强度，11 月攻核心，12 月融会贯通。**

## 1.3 时间投入是硬约束

- **每周课外不少于 8 小时**；零基础同学应按上限安排。
- 一学期预计完成 **150–200 道以上**编程题。
- 学期中不因假期停止练习节奏——中断一周，回来的成本远高于连续做题。

> ⚠️ 这门课的失败模式几乎只有一种：**前四周语法没过关，后面每一周都在还债。**

## 1.4 成绩与考核

课程采用**过程性学习 + 期末考核**：平时作业、阶段性月考（10 月 / 11 月 / 12 月初）、期末上机考试、笔试。具体权重与优秀等级条件以当学期正式教学通知为准。

需要现在就知道的两件事：

- **期末上机考试在第 16 周**，机考成绩对总评影响最大。
- **每一次机考都是 6 题 / 112 分钟**——三次月考与期末上机考试同一规格。
  这个数字现在就要记住：它决定了你平时该练到什么速度（平均一题不到 19 分钟，含读题与调试）。
- 月考的作用是**提前暴露问题**，不是筛人。第一次月考考砸的同学，只要在 11 月前补上语法与调试能力，仍然可以拿到很好的总评。

---

# 2 三个学习平台

## 2.1 OpenJudge：主战场

- 地址：<http://cs101.openjudge.cn/>
- 首次登录后点击"加入"，选择小组 **`cs101`**。
- 题目分三档：**E（Easy）/ M（Medium）/ T（Tough）**。选择题库 `计算思维算法实践`。
- 建议：**完成全部 Easy 和 Medium**，按自身情况挑战部分 Tough。

课程组另维护"每日选作"清单（仓库根目录 `ADS_problem_list_at_2026fall.md`），
每天一题，跨 OpenJudge / LeetCode / Codeforces / 洛谷，用来保持手感。

## 2.2 LeetCode：补充训练

- [热题 100（Top 100 Liked）](https://leetcode.cn/studyplan/top-100-liked/)
- 建议优先完成**除链表（14 题）与二叉树（15 题）之外**的部分——这两块属于《数据结构与算法》课程的范围。

## 2.3 小北智学：AI 助教

- 地址：<https://zx.pku.edu.cn>
- 搜索并加入当学期对应的课程卡片，即可用课程知识库做预习、复习和问题定位。
- 好问题的例子："这道题我用的是 O(n²) 的两重循环，n 到 10⁵ 会 TLE，有没有 O(n log n) 的思路？"
- **注意**：涉及作业提交、考试安排、课程事务的问题，**一律以教师和助教的通知为准**。

## 2.4 题解与教材

课程题解站：<https://fuynaloft.github.io/sol101/>

参考教材：

- 《Book_my_flight》、《计算机科学导论（第4版）》
- Python 学习：
  - [Python 3 教程](https://www.runoob.com/python3/python3-tutorial.html)、[C++ 教程](https://www.runoob.com/cplusplus/cpp-tutorial.html)、《Python编程：从入门到实践（第3版）》
- 算法类教材：
  - 《算法图解》、《算法基础与在线实践》、《算法笔记》（胡凡，曾磊）
  - 《算法导论 第3版》（Cormen, Leiserson 等）

---

# 3 课前准备

## 3.1 开发环境

| 语言 | 推荐 IDE | 配置指南 |
| ---- | ---- | ---- |
| Python | **PyCharm** | [Python 开发环境配置](../Python_Development_Setup_Mac_Windows.md) |
| C++ | **VS Code** | [VS Code 中编写第一个 C++ 程序](../Writing_First_C++_Program_in_VS-Code.md) |

**新电脑第一件事**：把用户名和目录名改成**英文或汉语拼音**。中文路径会在编译、
安装第三方包时引发难以定位的报错。

**第二件事**：电脑旁不放水杯。连续两年都有同学把水洒进笔记本，维修费接近整机价格。

## 3.2 盲打

上机考试是**限时**的：**112 分钟 6 题**，平均一题不到 19 分钟。
同样的思路，打字速度差一倍，能做完的题数就差两题。

- 目标：**40 字/分钟以上**，且不看键盘。
- 本学期第一周完成一次打字测试并记录成绩，第 8 周再测一次。

## 3.3 其他准备清单

- [ ] 注册 OpenJudge 并加入小组 `cs101`
- [ ] 加入小北智学课程卡片
- [ ] 安装 PyCharm（或 VS Code + Python 插件）
- [ ] 申请 O365（含 Teams）账号
- [ ] 完成一次盲打测试
- [ ] 阅读《图灵与 ACM 图灵奖》《IEEE 计算机先驱奖》
- [ ] 观看电影《模仿游戏》（The Imitation Game, 2015）

---

# 4 AI 基础：把 AI 当工具，不当替身

人工智能（artificial intelligence, AI）是对程序系统的研究，该程序系统在一定程度上能模仿人类的活动，如感知、思考、学习和反应。

> 人工智能是研究和开发能够执行通常需要人类智能的任务的技术和方法的学科，包括语音识别、图像理解、自然语言处理、机器人等[1] [2]。

早在1950年，艾伦·图灵提出了检验机器智能的“模仿游戏”（即**图灵测试**），检验机器是否能让人分不清其与人类对话的区别。1956年达特茅斯会议（**Dartmouth AI Workshop**）召开，被认为是人工智能领域的创始时刻，约翰·麦卡锡等人首次正式提出“人工智能”这一术语[1]。此后，AI发展经历了多次高潮与低谷，到21世纪依赖于强大的计算资源、海量数据和新算法的**深度学习**技术实现突破，推动AI进入广泛应用阶段。

人工智能（AI）与智能体（Agent）之间的关系可以概括为：“能力与载体”或“大脑与身体”的关系。AI 是智能体用来解决问题的核心技术（大脑），而智能体是将 AI 技术具体化、用于与环境交互的系统（实体）。

- **人工智能 (AI) —— 关注“能力”与“算法”**定义： 是一门学科或技术集合（如深度学习、NLP、计算机视觉）。它关注的是<mark>如何实现</mark>智能，比如如何识别语音、如何理解图像、如何制定策略。角色： 它是“大脑”。它负责处理数据、进行思考、学习和决策。
- **智能体 (Agent) —— 关注“行为”与“交互”定义：** 是一个具有自主性的系统（无论是软件还是硬件）。它关注的是<mark>如何应用</mark>智能去感知环境并完成任务。角色： 它是“实体”。它拥有感知器（眼睛/摄像头/键盘输入）和执行器（手/轮子/屏幕输出/API调用），负责与外界互动。

> **图灵测试**
> 在1950年，阿兰，图灵提出了图灵测试，这个测试提出了机器具有智能的一个定义。该测试的方法是，简单地比较人类的智能行为和计算机的智能行为。一个询问者对计算机和人类都提出一组问题，然后，询问者得到两组答案，但他不知道哪一组是来自人类，哪一组来自计算机。在仔细检查两组答案后，如果询问者不能肯定地说出哪一组来自人类，哪一组来自计算机，那么，计算机就通过了具有智能行为的图灵测试。

> **智能体**
> 智能体是一个能智能地感知环境、从环境中学习并与环境进行交互的系统。智能体可以分成两大类：软件智能体和物理智能体。
>
> **1.软件智能体**
> 软件智能体是一组用来完成特殊任务的程序。例如，有些智能系统能用来整理电子邮件e-mail)，能检查收到的邮件的内容，然后把它们归到不同的类别中(垃圾、不重要、重要非常重要等)。另外一个软件智能体的例子是搜索引擎，它搜索万维网，发现能提供与查询主题相关信息的网址。
>
> **2.物理智能体**
> 物理智能体(机器人)是一个用来完成各项任务的可编程系统。简单的机器人可以用在制造行业，从事一些日常的工作，如装配、焊接或油漆。有些组织使用移动机器人去做一些日常的分发工作，如分发邮件或明信片到不同的房间。移动机器人可以在水下探测石油。人型机器人是一种自治的移动机器人，它模仿人类的行为。虽然人型机器人只在科幻小说中流行，但是要使这种机器人能合理地与周围环境交互并从环境里发生的事件中学习，这里面还有很多工作要做。



**AI三大流派**

人工智能的发展思想分为三个主要流派：

- **符号主义（Symbolic AI）**：核心观点是通过符号表示和逻辑推理来实现智能。典型方法包括专家系统、搜索算法、逻辑推理（如一阶逻辑）、规划系统等。代表人物有艾伦·纽厄尔、赫伯特·西蒙、约翰·麦卡锡等。符号主义强调可解释性强，适用于有明确规则的任务（如数学推理、棋类游戏）。其代表成果包括20世纪80年代的专家系统（如DEC公司的XCON系统，显著提高了配置效率）以及IBM的棋类程序**深蓝（Deep Blue）**，1997年击败国际象棋冠军卡斯帕罗夫。但符号主义的缺点是学习能力弱，难以处理模糊信息，需要大量手工编码规则。

  > 虽然人工智能作为一门独立的学科是相对年轻的，但它还是经历了一段发展的时间。可以这样说，当2400年前希腊哲学家亚里多斯德发明了逻辑推理这个概念时，人工智能就开始了，接着莱布尼茨和牛顿完成了逻辑语言的定稿。乔治·布尔在19世纪逐步提出的布尔代数奠定计算机电子电路的基础。

- **连接主义（Connectionism）**：核心观点是通过模拟人脑神经元网络结构来实现智能，依赖数据驱动学习。主要方法是各种人工神经网络（ANN）和深度学习（如卷积神经网络CNN、循环神经网络RNN、Transformer等）。代表人物包括Geoffrey Hinton、Yann LeCun、Ilya Sutskever、David Rumelhart等。约翰·霍普菲尔德（John Hopfield）与Geoffrey Hinton于2024年获得诺贝尔物理学奖，以表彰他们在神经网络领域的奠基性贡献。连接主义流派引领了近年来AI的主要突破（“强数据、弱规则”）。其代表成果包括最早的单层感知机（Perceptron，1958年）以及1986年Rumelhart等人提出的**误差反向传播算法**（Backpropagation），使多层神经网络得以高效训练。现代连接主义系统在图像识别、语音识别、自然语言处理、自动驾驶等领域均取得了巨大成功，例如Google的AlphaGo和各种视觉模型、OpenAI的GPT系列语言模型等[3]。

- **行为主义（Behaviorism）**：在AI领域常指“机器人行动派”或强化学习思想。其核心思想是智能体通过与环境交互并根据反馈（奖惩）自主学习，无需事先假设内部知识结构。代表人物有罗德尼·布鲁克斯（Rodney Brooks）、李开复等。行为主义AI强调“行动优先”，对现代机器人学和强化学习影响深远。典型应用是基于强化学习的系统，如**AlphaGo/AlphaZero**（通过自我博弈学习围棋策略）和自动驾驶等。行为主义流派下的智能体可视为通过试错和环境反馈来优化决策。



**AI的三要素：算法、算力和数据**

人工智能的发展依赖于三大要素：**算法**（Algorithm）、**算力**（Compute）和**数据**（Data）。

- **算法（灵魂）**：不同任务类型对应不同算法范式。常见分类包括监督学习（使用标注数据训练模型进行分类或回归）、无监督学习（从无标签数据中挖掘结构，如聚类）、强化学习（使用奖惩机制优化策略）。例如，逻辑回归用于分类（监督学习），KMeans用于聚类（无监督学习）。机器学习和深度学习的算法不断演进，引入了多层神经网络、注意力机制等创新架构。
- **算力（引擎）**：深度学习模型往往需要巨大的计算资源。GPU/TPU等高性能硬件使得训练大规模神经网络成为可能。以PyTorch为例，我们可以简单检测当前硬件环境中GPU或Apple MPS的可用性。
- **数据（燃料）**：训练模型需要大量高质量的数据。监督学习尤其依赖标注数据。例如，李飞飞等人在2006年发起的ImageNet计划收集了数千万张图像，并依托众包标注创造了包含1400万张标注图片的大型数据集，大大推动了计算机视觉算法的发展。与此同时，无标签数据也通过自动记录等方式提供了海量信息，可用于无监督学习。总之，算法、算力与数据三者共同构成AI系统的基础。



## 4.1 几个必须分清的词

| 概念 | 一句话解释 |
| ---- | ---- |
| 人工智能 AI | 让机器完成通常需要人类智能的任务，是最大的那个圈 |
| 机器学习 ML | 从数据中学出规律，而不是把规则写死在代码里 |
| 深度学习 DL | 用多层神经网络做机器学习（第 15 周展开） |
| 大语言模型 LLM | 在海量文本上训练的、以"预测下一个词"为目标的深度模型 |

四者的关系是**层层包含**：AI ⊃ ML ⊃ DL ⊃ LLM。

```
   ┌───────────────── 人工智能 AI ─────────────────┐
   │  ┌────────────── 机器学习 ML ──────────────┐  │
   │  │  ┌─────────── 深度学习 DL ───────────┐  │  │
   │  │  │        大语言模型 LLM             │  │  │
   │  │  └───────────────────────────────────┘  │  │
   │  └─────────────────────────────────────────┘  │
   └───────────────────────────────────────────────┘
```



## 4.2 如何使用 AI：从对话到智能体

人工智能（AI）是一个广泛的技术领域，大语言模型（LLM）是当前许多 AI 应用的核心。AI 并未完全替代搜索引擎，但正在成为获取信息、分析问题和完成工作的主要入口。对于最新消息、重要数据和专业结论，仍应查阅官方网站、原始资料或其他可信来源。

本文按照由浅入深的顺序，分三个阶段介绍 AI 的使用方法：先通过浏览器与大语言模型对话，再上手 AI 智能体，最后学会对比和选择高性能智能体。建议按顺序学习，掌握前一阶段后再进入下一阶段。

### 4.2.1 通过浏览器与大语言模型对话

初学者可以从网页版 AI 助手开始，例如：

- [通义](https://www.qianwen.com/)
- [DeepSeek](https://chat.deepseek.com/)
- [Kimi](https://www.kimi.com/)
- [豆包](https://www.doubao.com/)

提问时，尽量包含以下信息：

- 背景：你正在做什么。
- 目标：希望 AI 完成什么。
- 约束：时间、格式、篇幅或技术限制。
- 输出：希望得到步骤、表格、代码还是完整文稿。

例如：

> 我是 Python 初学者。请解释什么是列表，并给出三个从简单到复杂的例子。不要使用尚未介绍的高级语法。

对于会随时间变化的信息，可以要求 AI 搜索资料、注明日期并提供来源链接。AI 可能给出看似合理但并不正确的回答（即“幻觉”），重要结论仍需人工核对。

### 4.2.2 使用 AI 智能体

> 提示：安装或使用中遇到问题时，可以先用阶段 1 的方法向 AI 询问解决方案。

智能体（Agent）不仅能够与 LLM 对话，还可以在用户授权后读取文件、修改代码、执行命令和调用其他工具。它通常由模型、上下文、工具、权限以及负责协调这些部分的智能体框架组成。

OpenCode、Claude Code 和 Codex 都属于智能体工具。它们并不是 LLM 本身，而是连接模型与计算机环境的工作平台。

使用智能体时应遵守以下原则：

- 在专用工作目录中启动，并使用 Git 或备份保护重要文件。
- 执行前说明目标和约束，执行后检查差异与测试结果。
- 谨慎批准删除文件、安装软件、发送数据等高风险操作。
- 不要把密码、API Key 或隐私数据写入提示词、代码仓库或公开文档。

#### 4.2.2.1 使用 OpenCode 体验免费模型

访问 [OpenCode 下载页面](https://opencode.ai/download)，可以选择终端界面或桌面应用。

OpenCode 是智能体客户端，不是一个固定的免费 LLM。它可以连接不同的模型服务。[OpenCode Zen](https://opencode.ai/docs/zen/) 当前提供部分限时免费模型，也提供按量付费模型。免费模型和使用政策可能调整，部分免费服务可能保留数据用于改进模型，因此不要提交个人信息、密钥或保密材料。

#### 4.2.2.2 使用 Claude Code 和付费 API

复杂任务通常需要能力更强的模型，但价格、速度和效果需要综合考虑。排行榜只能作为参考，因为排名会随测试集、智能体框架、模型版本和评测时间而变化。

可以访问 [Arena Agent Leaderboard](https://arena.ai/leaderboard/agent) 查看近期评测结果。本文选择“Claude Code + DeepSeek API”作为配置示例，主要考虑使用方便和成本，而不是固定名次。

##### 安装 Claude Code

laude Code 官方当前推荐使用原生安装程序，安装后会在后台自动更新。macOS、Linux 或 WSL 执行：

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

Windows PowerShell 执行（Windows CMD 的命令见官方文档）：

```powershell
irm https://claude.ai/install.ps1 | iex
```

也可以用 npm 全局安装（需要 Node.js 22 或更高版本，不要加 `sudo`）：

```bash
npm install -g @anthropic-ai/claude-code
```

npm 安装不会像原生安装那样可靠地自动更新，升级时执行 `npm install -g @anthropic-ai/claude-code@latest`。

安装完成后验证：

```bash
claude --version
claude doctor
```

其他安装方式参见 [Claude Code 官方安装文档](https://code.claude.com/docs/en/setup)。

##### 接入 DeepSeek API

配置前请查看最新的 [DeepSeek × Claude Code 官方集成指南](https://api-docs.deepseek.com/zh-cn/quick_start/agent_integrations/claude_code)。

首先前往 [DeepSeek Platform](https://platform.deepseek.com/api_keys) 创建 API Key，然后编辑用户配置文件：

- macOS、Linux：`~/.claude/settings.json`
- Windows：`%USERPROFILE%\.claude\settings.json`

```json
{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "env": {
    "ANTHROPIC_BASE_URL": "https://api.deepseek.com/anthropic",
    "ANTHROPIC_AUTH_TOKEN": "YOUR_DEEPSEEK_API_KEY",
    "ANTHROPIC_MODEL": "deepseek-flash[1m]",
    "ANTHROPIC_DEFAULT_OPUS_MODEL": "deepseek-flash[1m]",
    "ANTHROPIC_DEFAULT_SONNET_MODEL": "deepseek-flash[1m]",
    "ANTHROPIC_DEFAULT_HAIKU_MODEL": "deepseek-flash",
    "CLAUDE_CODE_SUBAGENT_MODEL": "deepseek-flash",
    "CLAUDE_CODE_EFFORT_LEVEL": "max",
    "CLAUDE_CODE_AUTO_COMPACT_WINDOW": "786432"
  }
}
```

将 `YOUR_DEEPSEEK_API_KEY` 替换为真实密钥。该文件包含明文密钥，不能提交到 Git 或发送给他人。macOS、Linux 用户还可以限制文件权限：

```bash
chmod 600 ~/.claude/settings.json
```

随后启动：

```bash
claude
```

启动后可以先布置一个简单任务进行验证，或输入 `/status` 查看当前模型与连接状态。

以上是官方指南当前的推荐配置，默认全部使用 `deepseek-flash`（DeepSeek-V4.1-Flash，1M 上下文）。旧名 `deepseek-v4-flash` 仍可调用，但会被转到 V4.1-Flash。复杂任务如果需要更强的模型，可以把前三项改为 `deepseek-v4-pro[1m]`，费用按 Pro 计。

DeepSeek 当前会将以 `claude-opus` 开头的模型名映射到 `deepseek-v4-pro`，将以 `claude-sonnet` 或 `claude-haiku` 开头的模型名映射到 `deepseek-flash`。这些名称和规则可能变化，使用前应重新核对官方集成指南。

### 4.2.3 对比并使用高性能智能体

第三阶段不是简单地“购买排名前三的模型”，而是根据任务选择合适的智能体系统。

排行榜中的 `Claude Fable 5.1 (Max)`、`GPT 6 Astra (Max)` 等名称可能同时包含模型版本、推理强度和智能体配置，不一定是可以直接填写的 API 模型 ID。应通过官方订阅、官方 API 或相应服务获得访问权限。

可以让两到三个候选智能体完成同一个真实任务，然后比较：

- 结果是否正确、完整；
- 是否遵守指令；
- 工具调用和代码执行是否成功；
- 完成速度与人工修改量；
- API 成本和数据安全要求。

推荐的学习路线是：先掌握浏览器对话，再使用 OpenCode 等工具体验智能体，随后学习 Claude Code、Codex 等更完整的工作流，最后通过真实任务选择适合自己的高性能模型和智能体。



## 4.2 大模型为什么会"一本正经地胡说"

LLM 的训练目标是"**下一个词是什么最合理**"，不是"这句话是不是真的"。所以：

- 它**擅长**：解释概念、翻译报错信息、给出代码骨架、指出常见坑。
- 它**不可靠**：具体的题号与链接、精确的数值结论、课程事务、没见过的新题的正确解法。

> 本课程有一条实测经验：**AI 生成的 OJ 题号经常是错的**。凡是题号，一律自己打开链接确认。

## 4.3 AI 的正确用法：三个可以，三个不可以

**可以**：

1. 让 AI **解释**你不懂的报错、语法、算法思想；
2. 让 AI **审查**你已经写完的代码，问"这段代码在什么输入下会出错"；
3. 让 AI **出题**：让它构造边界数据，然后你自己去验证。

**不可以**：

1. ❌ 直接让 AI 写作业代码然后提交——你没学到，机考时没有 AI；
2. ❌ 相信 AI 给出的题号、成绩规则、考试安排；
3. ❌ **在考试中使用任何 AI 工具**，包括本地模型和 IDE 的智能补全插件。

> ⚠️ **红线**：期末上机考试**禁止任何 AI 工具**；**无法解释自己提交的代码，按学术不端处理**。
> 这条规则从第 1 周就生效于你的学习方式：**每一行你提交的代码，都要能讲清楚为什么这么写。**

## 4.4 一个自检问题

每次借助 AI 完成一道题后，关掉窗口，**从空文件重写一遍**。写不出来，说明这道题没做完。

---

# 5 第一个 Python 程序

## 5.1 Hello, World

```python
print("Hello, World!")
```

## 5.2 输入的三种基本形态

OJ 的输入永远来自标准输入。三种最常见的形态：

```python
# 形态一：一行一个整数
n = int(input())

# 形态二：一行多个整数，空格分隔
a, b = map(int, input().split())

# 形态三：一行一个整数列表
nums = list(map(int, input().split()))
```

`input()` 读回来的**永远是字符串**，末尾不含换行符。忘了 `int()` 是初学者第一大坑：

```python
a = input()          # 输入 3
b = input()          # 输入 4
print(a + b)         # 输出 34 —— 字符串拼接，不是 7
print(int(a) + int(b))   # 输出 7
```

## 5.3 输出的格式控制

```python
print("Hello", "World")            # Hello World（默认空格分隔）
print("Hello", "World", sep="-")   # Hello-World
print("a", end="")                 # 不换行
print(f"{3.14159:.2f}")            # 3.14
print(*[1, 2, 3])                  # 1 2 3   —— 解包，OJ 输出数组的标准写法
```

## 5.4 第一道 OJ 题：E02750: 鸡兔同笼

**E02750: 鸡兔同笼**，<http://cs101.openjudge.cn/practice/02750/>

> 笼子里有鸡（2 只脚）和兔（4 只脚），已知脚的总数 a，求最少和最多可能有多少只动物。

**分析**：

- 要动物**最少** → 尽量用兔（4 只脚一只动物）；
- 要动物**最多** → 全用鸡（2 只脚一只动物）；
- a 为奇数、或 a = 0 时无解，输出 `0 0`。

```python
a = int(input())
if a <= 0 or a % 2 == 1:
    print(0, 0)
else:
    least = a // 4 if a % 4 == 0 else a // 4 + 1
    most = a // 2
    print(least, most)
```

**为什么最少是 `a//4` 向上取整**：先全放兔子，剩下的 2 只脚只能再加一只鸡。
`a % 4 == 2` 时，`a//4` 只兔 + 1 只鸡 = `a//4 + 1` 只。

## 5.5 第二道：E02733: 判断闰年

**E02733: 判断闰年**，<http://cs101.openjudge.cn/practice/02733/>

闰年规则：能被 4 整除且不能被 100 整除，**或**能被 400 整除。（本题另有"不能被 3200 整除"的附加条件，务必看清题面。）

```python
a = int(input())

if a % 4 == 0:
    if a % 100 == 0 and a % 400 != 0:
        print('N')
    elif a % 3200 == 0:
        print('N')
    else:
        print('Y')
else:
    print('N') 
```



```python
y = int(input())
leap = (y % 4 == 0 and y % 100 != 0) or (y % 400 == 0 and y % 3200 != 0)
print("Y" if leap else "N")
```



> **本周最重要的一课**：先把**题面读完**，再动手。上面这道题绝大多数 WA 都来自漏看 3200。

---

# 6 本周练习

| # | 题目 | 平台 / 编号 | 考点 |
| - | ---- | ---- | ---- |
| 1 | 完成 OpenJudge 注册并加入 `cs101` 小组 | — | 环境 |
| 2 | 鸡兔同笼 | E02750 | 分支、整除 |
| 3 | 判断闰年 | E02733 | 条件表达式、读题 |
| 4 | 与 7 无关的数 | 02701 | 循环、取模 |
| 5 | 大小写字母互换 | E02689 | 字符串遍历 |
| 6 | 整数的个数 | E02676 | 计数 |
| 7 | 提交一次盲打测试截图 | — | 课前准备 |
| 8（选做） | Hangover | E01003 | 浮点累加、while |

**思考题**：

1. `input()` 与 `sys.stdin.readline()` 有什么区别？为什么后者在大数据量时更快？
2. 为什么 `print(0.1 + 0.2 == 0.3)` 输出 `False`？（第 4 周会正面回答）
3. 让 AI 助教解释一道你已经 AC 的题，看看它的思路和你的有什么不同——差异往往就是你的知识盲区。

---

# 7 小结

1. 这门课是**练出来的**：每周 8 小时课外时间、一学期 150–200 题，是达标线不是上限。
2. 三个平台各司其职：**OpenJudge 练题、LeetCode 补强、小北智学答疑**。
3. AI 是**加速器不是替身**。可以问原理，不可以代写；**考试中一律禁用**；**讲不清自己的代码 = 没做这道题**。
4. `input()` 返回字符串；忘记 `int()` 是第一大坑。
5. 读题读完整——本周两道例题的主要失分点全在读题上。

**下周预告**：把开发环境往下挖一层——**虚拟机、Linux Shell**，以及为什么每个程序员都要会用命令行。
