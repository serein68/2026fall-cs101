# PLAN · 唯一任务清单与决策记录

> 这是 Claude 与 Codex 共享的**唯一任务事实源**。人拍板任务与优先级；
> 两个 agent 认领任务、更新状态、署名。状态流转：`Backlog → In progress → Review → Done`。
> 每条任务用一个 `T-<编号>` 标识，交接与提交信息里引用它。
>
> **格式硬约束**：每行恰好 5 列；追加复核结论写进备注列、用 ` — ` 分隔，
> **不要**用 `| ` 另起一格 —— 超出表头的单元格在 GitHub 渲染时会被直接丢弃。
> 描述里含 `|` 的片段要转义（`\|`）。

## 状态看板

| ID | 任务 | 状态 | 负责 | 关联提交 / 备注 |
| --- | --- | --- | --- | --- |
| T-001 | 按课程指南「课程安排」生成第 1–16 周讲义（16 份 `.md`，含完整题解、例题、作业、思考题） | Done | Claude | 约 8900 行；209 个 python 代码块全部 `ast.parse` 通过；讲义里带注释输出的片段已逐条实跑核对。2026-09-01 人确认验收 |
| T-002 | 为第 1–16 周制作课件（16 份 `.pptx`，**451 页**），排版做成可重复生成的脚本 | Done | Claude | 引擎 `courseware/deck.py`（与 cs201cq 共用、字节一致）+ 内容 `content/wNN.py`；渲染检查 451 页无越界。页数演进：439 →（Q-7 拆页）443 →（T-012 补题）451；T-011 已补齐至 443 页的 PowerPoint 复核，新增/改动的 29 页见 **T-013**。2026-09-01 人确认验收 |
| T-003 | 第 16 周期末上机考试命题方案：覆盖矩阵、样卷 6 题（题面+样例+参考解答+数据构造+评分）、备选题库 | Done | Claude | T3/T4/T5/T6 各做过 200–400 组随机对拍；T1/T2 已逐样例实跑；T-009 红队样卷已补齐。2026-09-01 人确认验收 |
| T-004 | 搭建 Claude⇄Codex 协作脚手架（本目录 + `tools/handoff.py`） | Done | Claude | 协作记录、交接包生成与四道闸门编排已投入使用。2026-09-01 人确认验收 |
| T-005 | 一致性闸门 `tools/verify_courseware.py`（**11 项检查**，含离线题号↔题名核对） | Done | Claude | 第 7 轮加固：263 处题号覆盖、已核实题名覆盖层，并比较重建产物与已提交 PPTX 的可见文本；第 9 轮新增第 9 项**机考规格**（6 题 / 112 分钟 / 15+15+15+15+20+20）；第 10 轮新增第 10 项**版面标记**（放映稿上不得印出 `**` / 反引号；bullet 不得以空白开头）；第 11 轮把第 9 项扩到**难度梯度表不得列分数**；第 12 轮新增第 11 项**列对齐**（等宽图里不得有跨中文字数的对齐列）。2026-09-01 人确认验收 |
| T-006 | 讲义代码语义闸门 `tools/check_note_code.py`（把讲义实现原样抽出来对拍） | Done | Claude | **78 项**语义用例（第 9 轮 +5），包含 W05 月考与 W16 样卷参考解答；变异自检覆盖正序背包、排序键缺列与边界差一。2026-09-01 人确认验收 |
| **T-007** | **用 Microsoft PowerPoint 导出 PDF 并逐页缩略图复核 439 页课件** | **Done** | **Codex（macOS）** | `4a5d535` — PowerPoint 16.112.2 导出 16 份 PDF，逐页复看 439 / 439 页无版面缺陷；Claude 复核：逐周页数（含 W15=24 这一修正后的值）与实际 `.pptx` 及 README 三方一致、提交未触及 `courseware/`、60 张浅灰页均为有标题的 section 分隔页 。ℹ️ 该复核针对的是当时的 439 页版本；此后 Q-7 拆出的 8 张页由 **T-011（`5c63737`）** 单独补做了 PowerPoint 复核，**443 页现已全部覆盖** |
| T-008 | 联网逐条核实讲义引用的 OJ / LeetCode 题号、题名、链接 | Done | Codex | `b2e6e3e` — 联网核实 56 个 OpenJudge + 19 个 LeetCode 端点，编号与链接全部正确；唯一过时题名 12559「最大最小整数 v0.3」已修正，讲义 / `content/w06.py` / 重生成课件三处同改。Claude 复核：符合红线 2（课件文本与从源重建的结果逐字一致）；并据此加固闸门第 7 项 |
| T-009 | 红队样卷：为 W16 六道题构造能让参考解答 WA / TLE 的数据 | Done | Codex | `cfc7c94` — `tools/redteam_exam.py`，每题至少一个 WA / TLE / MLE 证据，固定反例写入讲义 5.7。Claude 复核：改动符合红线 2；修掉其中 3 处**恒真或硬编码**的"证据"，并补上此前缺失的一环 —— 让 fixture 去跑**讲义自己的参考解答** |
| T-010 | 闸门自身失败路径的回归测试（`tools/test_gate.py`） | Done | Codex + Claude | `3b42b21`（Codex，7 项：临时副本 + 真实 CLI 入口）→ Claude 第 6 轮补 5 项至 **12 项**：反向对照、语料分支题名漂移、第 6 项页数、`deck.py` 溢出守卫、渲染判据阈值。回退闸门修复的变异自检 **7/7 全部变红**；已并入 `--verify` |
| T-011 | 对 Q-7 修复后新增/改动的 8 张课件页补做 PowerPoint 复核 | Done | Codex（macOS） | `5c63737` — PowerPoint 16.112.2 导出并逐张复看 W14 p19-20、W16 p20-21 / p23-24 / p28-29，无缺字、方框、重叠、裁切、错位或标记泄漏。Claude 复核：这 8 个页码与 `content/` 里带「(1/2)/(2/2)」标记的拆分页**逐一对上**（W16 的 20/21/23/24/28/29 因前序拆分而位移，对得上说明渲染的是当前版本）；提交仅动 `collab/` |
| **T-012** | **把三次月考与期末机考统一到 6 题 / 112 分钟：W05 补 T6、W14 补 T5/T6、W16 消除时长自相矛盾，并把该规格做成闸门第 9 项** | **Done** | **Claude** | 本轮 — 人给出事实：**每次机考（含三次月考与期末机考）都是 112 分钟、6 题**。W05 样卷 5→6 题（新增 T6 补码计算器，补上全卷唯一没被考到的 W3 知识点）、W14 讲评 4→6 题（新增 T5 二分答案、T6 扩展域并查集）、三份样卷分值统一为 15+15+15+15+20+20；W16 原文同时写着「2025 秋为 112 分钟」与「约 120 分钟」，已消歧。3 段新参考解答均与独立参照模型对拍；闸门第 9 项做过 4 项变异自检 + 反向对照 |
| T-013 | 对 T-012 新增/改动的 **29 张课件页**补做 PowerPoint 复核 | Done | Codex（macOS） | PowerPoint 16.112.2 — 已从 `HEAD^` 与当前 `content/wNN.py` 做序列对齐，独立复算为 W01 2 + W05 13 + W14 11 + W16 3 = 29 页，与列出的页码逐一一致；导出当前四份 PPTX 为 PDF 并逐张复看 29 / 29，无缺字、方框、重叠、裁切、错位、意外空白或非预期 Markdown 标记泄漏。临时产物 `/private/tmp/cs101-t013-powerpoint-review/` — **Claude 复核：接受页码与页数（四份 PDF 页数三方一致、29 个页码可从 `content/` 独立复算、提交仅动 `collab/`），但"29/29 无标记泄漏"这一条不成立**：W05 p18 的 ``**负数不能用 `bin(x)`**`` 把反引号原样印了出来。追查发现是 `deck.py` 的通病，全仓库 9 处、6 份课件，其中 W16 p22、W07 p28 等 7 处早于 T-012，在 T-007（439 页）与 T-011（8 页）里也漏过。另有 W14 p32 一处空 bullet（T-012 引入）。两类都已在 **T-014** 修掉并进闸门 |
| **T-014** | **修 `deck.py` 的 Markdown 记号泄漏，并把「放映稿上不得出现 `**` / 反引号」做成闸门第 10 项** | **Done** | **Claude + Codex（PowerPoint）** | 本轮 — 复核 T-013 时发现。`_SEGMENT` 把 `**…**` 整段吞掉不再往里拆（反引号泄漏），且 `[^*]+` 匹配不上含 `*` 的粗体内容（`**` 自身泄漏）。共 9 处、6 份课件；**第三处（W02 p27 的 ``**`[[0]*n]*m` 别名陷阱**``）是闸门自己抓出来的，不是人先看见的**。同时修掉 W14 p32 用全角空格伪造续行导致的空 bullet。第 10 项做过 3 项变异自检 + 反向对照，`test_gate` 补 3 条（19 → 22）。Codex 以 PowerPoint 16.112.2 复看 W02 p27、W03 p27、W05 p18、W07 p28、W14 p32、W16 p22，共 6 / 6；`python-pptx 1.0.2` 下第 6 项与第 10 项 B 层均实际通过，且两条 B 层变异测试均会拒绝回归。Claude 复核：6 个页码用**另一条路**（从 `HEAD~2` 旧源按旧引擎语义反推）独立复算，与 Codex 的产物 diff 结果完全一致；提交仅动 `collab/`。`deck.py` 与 cs201cq 的分叉已由人同步消除（Q-9 已销账） |
| **T-015** | **删掉难度梯度表里的分数列（W05 p10、W16 p13），并把这条做进闸门第 9 项** | **Done** | **Claude** | 本轮 —— 人指出「机考之后另有成绩核算办法」，梯度表里那一列 15分/20分 是同一事实的**第五个写法**，且会跟真正的核算口径打架。删的只是课件 ascii 表的第二列（`content/w05.py`、`content/w16.py`）；讲义侧的梯度表本来就只有星级、无分数列，删后两侧结构反而对齐了。顺手修掉 W05 梯度表 T3/T4 那一列**早于本轮就存在的 1 格错位**（`--` 在第 55 列而非 54，`HEAD~2` 即如此），现在 6 行的星列与 `--` 列都严格对齐。闸门第 9 项扩出「难度梯度表不得出现分数」判据，做过 2 项变异自检（课件侧 / 讲义侧各一）+ 1 项反向对照（梯度表标题里的「112 分钟」不得误判为分数），`test_gate` 22 → **25 项**。总页数不变（451）|
| T-016 | 对 T-015 改动的 **2 张课件页**（W05 p10、W16 p13）补做 PowerPoint 复核 | **Done** | Codex（macOS） | PowerPoint 16.112.3 实际复看：两页分数列均已删；W16 p13 的星列与两处 `--` 正常对齐，但 **W05 p10 未通过**，六行 `--` 随中文文本宽度左右漂移，未落在同一列。源字符串的等宽填充未能保证 PowerPoint 下的实际字形宽度 `c9c4915`。**Claude 复核：接受，并且这个"未通过"比通过的那一页更有价值**——本机 LibreOffice 复测同一页，6 个 `--` 的 x 极差 **36.85 pt**；而"通过"的 W16 p13 只有 2 行、前缀汉字数只差 2，极差 1.02 pt，**它不是对的，只是漂得还看不出来**。根因见 T-017 |
| **T-017** | **难度梯度改用表格；`ascii` 图里禁止跨中文对齐（闸门第 11 项）** | **Done** | **Claude** | 本轮 —— T-016 的根因不是"填充算错了"，是**汉字宽度对不上任何等宽拉丁字体**：汉字字身宽 1 em，Consolas 约 0.55 em，两个空格 = 1.10 em 永远补不出一个汉字。只要一列前面隔着中文，各行中文字数不同，那一列就必然漂 —— 这是几何事实，不是实现缺陷。所以不再"把填充算得更准"，而是**换掉排版手段**：W05 p10、W16 p13 的难度梯度改成 `table`（PowerPoint 按单元格排版，不依赖任何字宽模型），实测四列的 x 极差全部 **0.00 pt**。顺带修掉 W16 p4「算法主线」流程图：`贪心` 下方那条竖线实测偏 **39.4 pt**（`|`/`v` 在 x=341.9，`贪心` 在 x=302.5），改成每条分支各占一行、只保留行首那一列（前缀纯 ASCII，实测 0.0 pt）。闸门新增第 11 项**列对齐**，做过 2 项变异自检 + 1 项反向对照；同轮补上一个自己挖的坑：第 9 项的梯度表扫描原先只认 `ascii`，改成 `table` 后表格单元格里再写分数会**静默放过**，已改成按页标题认、摊平所有字符串。`test_gate` 25 → **28 项**。总页数不变（451）|
| T-018 | 修剩下 **7 张**同类等宽图（方框里写中文、右边框对不齐） | Todo | 待定 | T-017 实测的 `|` 列 x 极差：W01 p17 **9.2pt**、W02 p5 **7.8pt**、W03 p6 2.4pt、W03 p10 3.5pt、W08 p10 **10.0pt**、W13 p11 **13.4pt**、W15 p12 0.1pt。都远小于判不通过的 36.85pt，但性质完全一样。已在闸门第 11 项的 `KNOWN_RAGGED` 里**按（模块, 标题）挂账**——挂账是为了让新写的 ascii 图立刻变红，不是为了假装这 7 张已经修好。每张都要单独决定改成表格还是改画法，属于教学图的版面决策，不宜混在修梯度表的那次提交里**2026-09-02 人已拍板：先不动，继续挂账。** 理由是实测偏移 0.1–13.4pt，都远小于判不通过的 36.85pt，而闸门每次输出都会报「7 张 T-018 挂账」，不会被遗忘。**触发条件**：下次因别的事要动 W01 / W02 / W03 / W08 / W13 / W15 的课件时，顺手把那一周的那张图一并修掉 —— 改法逐张选（本来就是表的改 `table`，本来是结构图的去掉右边框、只保留行首那一列）。改完需请 Codex 在 PowerPoint 下复看对应页 |
| T-019 | 对 T-017 改动的 **3 张课件页**（W05 p10、W16 p4、W16 p13）做 PowerPoint 复核 | Done | Codex（macOS） | Microsoft PowerPoint **16.112.3** 导出当前 W05（28 页）与 W16（40 页）PDF，实际逐张复看 3 / 3。W05 p10、W16 p13 的四列表格列边界稳定，六行星级及百分比完整；W16 p4 的 `|` 与 `+--` 同一竖线。未见缺字、方框、重叠、裁切、错位、意外空白或标记泄漏；临时证据 `/private/tmp/cs101-t019-powerpoint-review/`，不入库。**Claude 复核：接受**。三条独立对上：① 已提交课件里 W05 p10 与 W16 p13 确为 **7×4 原生表格**（不是补空格的等宽文本），6 行星级与 6 个百分比逐格齐全；② 页数 28 / 40 与我这边重建的结果一致，说明复看的是当前版本；③ 提交仅动 `collab/`，合并后闸门 `--render` 全绿（451 页）。**至此 T-015 → T-019 这一串（分数列 → 对齐 → 根因 → 闸门 → 复核）全部闭合** |
| **T-020** | **W06 补写 §3.4「交换论证：把排序键推出来，而不是猜出来」** | **Done** | **Claude** | 人指定 —— 原来只有一句定义（`w06.py` 的 `key` 页 + 讲义里两行引用），学生知道"有这么个论证"，但不知道**怎么用它推排序键**，而这才是考场上唯一用得上的部分。新增：三步骨架 / 为什么必须相邻 / **反过来用推 key** / 两种形态（相邻交换 vs 首元素替换）/ 两个前提（传递性、交换只影响这两项）。原 §3.4 顺延为 §3.5。讲义 +1 个可运行代码块（12559 的 key 推导 + 300 组全排列对拍，实测退出码 0、打印值与注释一致），课件 **27 → 32 页**，总页数 451 → **456**。`check_note_code` 补 1 项至 **79 项**：不只验最终答案，直接验骨架第 2 步 —— 逐个逆序对实测"换过来严格更大"，并断言样本里真的碰到了 200 次以上逆序对（否则等于空跑）。变异自检：把讲义里的 `cmp` 改成字典序降序 → 该项立刻变红 |
| T-021 | 对 T-020 新增的 **5 张课件页**（W06 p20–p24）做 PowerPoint 复核 | Done | Codex（macOS） | Microsoft PowerPoint **16.112.3** 导出当前 W06（32 页）PDF，实际逐张复看 5 / 5。p22 代码完整可读，与页脚留有清晰间距，无裁切；p23 经结构核对为 **3×3 原生表格**，三列对齐；p20 / p24 无空项目符号；`「」`、破折号及中文均未变方框。未见缺字、重叠、错位、意外空白或标记泄漏；临时证据 `/private/tmp/cs101-t021-powerpoint-review/`，不入库。 **Claude 复核：接受**。三条独立对上：① 直接读已提交的 `.pptx`：p23 确为 **3×3 原生表格**（与 Codex 报的数字一致），p20 / p24 各 6 条 bullet、**0 个空行**（即没有空项目符号 —— 这正是我这轮又犯、被第 10 项 A 层抓住的那个错），p22 是 23 行代码页；② 「未压页脚」这一条闸门第 8 项也能独立判（版心底部判据），合并后 `--render` 全绿；③ 提交仅动 `collab/` 三个文件。W06 32 页、全仓库 456 页三方一致 |
| **T-022** | **改写 W08 §2.2「递归深度限制」—— 原文描述的是 CPython ≤3.10 的行为** | **Done** | **Claude** | 人问 p9 那句「调大限制只解除了 Python 层的检查，C 栈仍有限」是什么意思，顺手实测发现**讲义给的兜底办法在 3.12 上对最常见的场景不成立**。本机（CPython 3.12.3 / Linux，已 `setrecursionlimit(1<<20)`）实测：纯 Python 递归 **30 万层照跑**（连 32 KB 的线程栈都够），而递归**穿过 C 代码**（`@lru_cache` —— W08 本周就在教的记忆化）**3331 层封顶**，且 `threading.stack_size(64MB)` **抬不动**。根因：3.11 起 Python→Python 调用在解释器主循环内展开、不再占 C 栈；3.12 又给 C 层递归单设了一道 `setrecursionlimit` 管不到的上限。§2.2 改写为「两道墙」结构 + 两张实测表（**明写环境与版本，声明是实测值不是语言规范**）+ 可自测的二分探测代码 + 显式栈结论；`threading` 那段保留但改成「要知道它治的是哪种病」，逐条写明 ≤3.10 有效 / 3.11+ 没必要 / 穿过 C 的没用。同步改了 §5.3 错误表那行与小结第 2 条。课件 **26 → 29 页**（1 页拆成 4 页），总页数 456 → **459**。`check_note_code` 补 1 项至 **80 项**（断言的是两道墙的**相对关系**，不是 3331 这个数，并按 `sys.version_info` 对 ≤3.10 跳过）；2 项变异自检：去掉 `lru_cache` → 两墙并一墙、当场变红；把 `depth_iter` 写回递归 → 变红 |
| T-023 | 对 T-022 改动的 **4 张课件页**（W08 p9–p12）做 PowerPoint 复核 | Done | Codex（macOS） | Microsoft PowerPoint **16.112.3** 导出当前 W08（29 页）PDF，实际逐张复看 4 / 4。p9 为 3×4 原生表格，首格按设计为空，`✅` / `❌` 正常；p10 为 3×3 原生表格，`1 << 20` 正常；p11 无空项目符号；p12 的 22 行代码完整可读、未压页脚或裁切。未见缺字、方框、重叠、错位、意外空白或标记泄漏；临时证据 `/private/tmp/cs101-t023-powerpoint-review/`，不入库。 **Claude 复核：接受，并补了一条它缺的绿线**。① 直接读已提交的 `.pptx`：p9 = 3×4 表格且表头首格确为 `''`（与设计一致）、`✅` `❌` 两个符号都在；p10 = 3×3；p11 6 行 bullet、0 空行；p12 21 行代码、「未压页脚」这一条闸门第 8 项能独立判，`--render` 全绿。② 顺带修的 `~$*.pptx` 豁免**方向正确、位置也对**（全仓库只有第 1 项这一处 glob `*.pptx`，第 6 / 8 / 10 项都走 `build_all.WEEKS` 映射，不受影响）。③ **但它没有被测试守住**：把那 5 行豁免删掉，Codex 新加的 `test_orphan_pptx_fails_pairing` 照样通过（实测确认）—— 那条守的是**旧行为**（普通孤儿必须红），是正确的反向对照，但**新行为自己没有绿线**。已补 `test_office_lock_file_is_not_flagged`：副本里放一个 `~$…pptx`，闸门必须仍绿且不出现「孤儿课件」；撤掉豁免后该条当场变红。`test_gate` 30 → **31 项** |
| **T-024** | **改写 W10 §2.2「DP 的两个前提」——「最优子结构」原来只有表里一行** | **Done** | **Claude** | 人问「W10 讲最优子结构？」。查下来：讲了，但只讲了半句 —— 表格下面有一整段展开**重叠子问题**，**最优子结构一个字都没展开**；而且它的反例「最长路（有环时不成立）」在第 10 周**学生验不了**（全仓库「最长路 / 图论 / 邻接表」在 W10 之前一次都没出现，图要到 W12）。新写的核心是一句话：**最优子结构是状态定义的性质，不是题目的性质** ——"这题没有最优子结构"十有八九是"我的状态少了一维"。配一个**能手算的反例**：4 行数字三角形（只有 8 条路径，可以逐条列出来）+ "路径和必须是奇数"，朴素状态 `dp[i][j]` 会返回**无解**（底行 `[20,22,24,22]` 全是偶数），而答案是 17；错在哪一步可以精确指出来 —— 走到第 4 行那个 `4` 时，上一行候选是 13（奇）和 18（偶），朴素状态只留 18，而 `17 = 13 + 4`。补一维 `dp[i][j][p]` 就对了。再给一条可操作的检验：**把子问题的最优解换成次优解，父问题会不会反而更好？**原来的「最长路」降为 **W12 §1.1 的回看**（那里现在补了一段：为什么它补多少维都救不回来，以及"同一张图把最长换成最短，最优子结构就有了"）。W10 课件 **25 → 29 页**，总页数 459 → **463**。`check_note_code` 补 1 项至 **81 项**（两头都断言：朴素状态**必须算错**、补一维**必须与暴力逐组一致**，外加反向对照"2000 组里朴素状态至少错 100 次"，否则反例说服力不够）；2 项变异自检：把补一维写坏 → 红；换成朴素状态碰巧也对的三角形 → 红 |
| T-025 | 对 T-024 改动的 **5 张课件页**（W10 p14–p18）做 PowerPoint 复核 | Done | Codex（macOS） | Microsoft PowerPoint **16.112.3** 在 macOS 图形会话中实际逐张复看当前 W10（29 页）的 5 / 5。p14 为 3×3 原生表格，三列稳定；p15 的 key 无裁切；**p16** 左侧三角形与右侧 8 条路径两栏未错位，`<- 唯一的奇数` 完整，两个路径栏内的 `=` 分别对齐；p17 的 12 行代码完整、未压页脚；p18 第 2 条按既有行为渲为 `▍` 引用块，其余 5 条均为正常 `•`，无空项目符号。五页未见缺字、方框、重叠、意外空白或 Markdown 标记泄漏。临时截图证据 `/private/tmp/cs101-t025-gui-review/`，不入库；其余 458 页本轮未动 **Claude 复核：接受**。① 直接读已提交的 `.pptx`：p14 = **3×3** 原生表格；p16 = 8 行等宽图；p17 = 11 行代码（我在交接里写的"12 行"是笔误，Codex 照抄了，不影响结论）；p18 = 6 条 bullet，**恰好第 2 条是 `▍`、其余 5 条是 `•`、0 空行**，与预期逐条对上。② 源侧闸门第 11 项（本轮刚把 `=` 补进记号表）对 p16 那张图判绿，与"两栏 `=` 各自对齐"这一目视结论同向。③ 提交仅动 `collab/` 三个文件，合并后 `--render` 全绿（463 页）。④ **这次是在 PowerPoint 图形会话里逐页看截图，不是导 PDF** —— 对版面而言这是比导出更硬的证据，已记入决策记录 |
| **T-026** | **把课件合成带旁白的教学视频：移植 `make_video.py`，先做 W01 / W02** | **Done** | **Claude** | 人指定 —— 对照 `dsa-modernization/courseware` 的同名脚本移植（章号 → 周次、`chNN` → `wNN`），其余一行未改：画面从**当前** `.pptx` 现导（LibreOffice → PDF → `pdftoppm`），语音按**讲稿文本的哈希**缓存，`video/wNN.timeline.json` 记下课件与讲稿的 sha256 供 `--check` 判「视频过期」。新写逐页旁白讲稿 `content/w01_narration.md`（32 节 / 5133 字）与 `content/w02_narration.md`（28 节 / 6011 字）——**一节对应一页，页数对不上直接报错**；题号与符号一律写成中文读法（「E 零二七五零」），因为是逐字送进 TTS 的。产物：W01 **16:46** / 24.5 MB，W02 **18:31** / 27.4 MB，均 1920×1080 / 30fps / AAC 48kHz，与 DSA 那套同规格；`--check` 两周均绿。抽帧核对过时间轴（12:35 处仍是 p25，与「p26 起于 12:36」一致）。入库口径沿用 DSA：讲稿 / 字幕 / 时间轴 / 720p 轻量版入库，1080p 成品不入库。⚠️ **本轮未动任何 `.pptx`**，因此 T-018 挂账的 W01 p17、W02 p5 两张等宽图**未触发**其「顺手修掉」的条件，仍挂账；也因此不需要 PowerPoint 复核。⚠️ 旁白是 TTS 合成的，`--check` 只验结构（同版、页数、字幕条数），**验不了念得对不对** |
| **T-027** | **补齐 W03–W16 的教学视频：14 份逐页旁白讲稿 + 合成，16 周全覆盖** | **Done** | **Claude** | 人指定 —— T-026 之后接着做完。新写 `content/w03_narration.md` .. `w16_narration.md`，**403 节**（加上 T-026 的 W01/W02 共 **463 节，与 463 页课件逐页对齐**，页数对不上直接报错）。讲稿按朗读稿写：题号、符号、公式一律中文读法。16 周视频合计 **4 小时 0 分 27 秒**，单周 11:44（W15）–20:09（W16），1080p 成品合计约 330 MB、720p 轻量版合计约 183 MB。`make_video.py --check` **16 / 16 全绿**；抽帧核对过 W16 的时间轴（10:30 处是 p22「T4 穿墙迷宫」，与时间轴 10:05–10:47 一致）。`make_video.py` 与 T-026 相比**一行未改**。⚠️ **本轮仍未动任何 `.pptx`**，T-018 挂账的 7 张等宽图未触发其条件，仍挂账；不需要 PowerPoint 复核。⚠️ 讲稿内容准确性闸门验不了（`--check` 只验同版、页数、字幕条数），**需要人抽听** —— 优先 W05 / W16 的考试规定页与 W14 p12 的学术诚信页 |
| **T-028** | **核实并替换 W05 / W14 / W16 月考样卷 T1–T6 里编造的题目，换成 cs101.openjudge.cn 「已结束的比赛」里的真题** | **Done** | **Codex（三轮 md）+ Claude（第四轮 pptx）** | 人发现：`202609_ADS_W05_October_Exam_Review.md`（含「T5. 电梯调度模拟」）、`202612_ADS_W14_AI_Literacy_Exam_Recap.md`、`202612_ADS_W16_Review_Final_Machine_Exam.md` 三份讲义里各自的 T1–T6，是 **T-001 批量生成第 1–16 周讲义时 AI 编造的原创题**，题面未标来源，且与 `2025fall-cs101/previous_exam_questions.md`、`2025fall-cs101/ADS_problem_list_at_2025fall.md` 现有题库对不上——全仓库搜「电梯」只在这三份讲义自身及其派生物（narration / 字幕 / pptx_builder 脚本）里出现。人给的真题来源：`http://cs101.openjudge.cn` 拉到页面最下方「已结束的比赛」，题号为 5 位数（如 `27351`），可在 `http://cs101.openjudge.cn/practice/<题号>` 打开/提交核实——**该站似乎只认 HTTP，Claude 侧的 WebFetch 会强制升到 HTTPS 导致 `ECONNREFUSED`，抓不到**，这也是人指示「让 Codex 来完成」的直接原因。人指示：这次要做的是**替换**，不是像 T-012 那样「追加」——18 道题全部要甄别是否为编造并换成真题；换题时要保留原题在样卷里覆盖的知识点与难度梯度（对照自检清单 §2、§3 …），同时同步改课件与任何交叉引用（如 W13 引用过「第 5 周月考样卷 T4 的筛法」——换题后这类引用要跟着改，或换一个仍成立的落点）。适用闸门第 7 项（题号↔题名离线比对）与红线 5（联网核实登记进 `VERIFIED_TITLES`，做法同 T-008）。**未决**：Q-10（见下）。⚠️ **`b3c14d2`（Codex，2026-09-22）第一次交付被人 + Claude 打回**：18 个真实题号已核实、登记进 `VERIFIED_TITLES`，三份课件的难度梯度总览表（唯一涉及月考样卷的那页）已换成真实题号/题名——**这部分是对的，予以保留**。但没做到「替换」：md 里只在正文最前面插了一张 6 行小表（题号/题名/考点/链接，**没有题意复述、没有参考解答**），旧的 18 道编造题**原文整段照旧保留**，仅把标题从 `## T1. xxx` 改成 `## T1.（旧草稿，已废弃）：xxx`——学生打开文件看到的主体内容仍然是假题的完整题解。对比本仓库其他周引用真实 OJ 题的写法（如 W12「04115: 鸣人和佐助」：题号+链接+题意复述+参考解答），这次给真题的规格明显不够。另外 **W13 那处交叉引用「第 5 周月考样卷 T4 的筛法」完全没处理**——它现在指向的是标了「已废弃」的假题。**且这次提交完全没走协作流程**：没碰 `collab/PLAN.md`（状态一直停在 Backlog）、没写 `NOTES-codex.md`、没追加 `HANDOFF.md`——这条硬约束（README「开工前先在 PLAN.md 认领任务」「交回时必须附…」）本身也要在重做时补上。详见 `collab/NOTES-claude.md` 2026-09-22 第 22 轮、`collab/HANDOFF.md` 同日条目。⚠️ **`0414dcb`（Codex，2026-09-22）第二次交付：部分接受，仍有缺口，继续打回**。这次做对了：18 道真题在三份 md 里都补上了题意复述（除 W14/W16 各自的 T6），旧的 18 道编造题挪进 `<!-- -->` HTML 注释、不再是可见正文，W13 交叉引用改成回看 W04 埃氏筛，`HANDOFF.md`/`NOTES-codex.md` 也照协作流程写了。**但复核发现两个本轮新引入的缺口**：① `tools/check_note_code.py` 只改了 W05 的语义测试（`t_w05_exam_solutions`），W14/W16 的测试函数（`t_w16_reference_solutions` 等）**没有改**——它们靠 `stdin_solutions()` 正则抓 `def solve(): ... solve()` 这种写法，而旧的 18 道编造题全是这种写法、新写的 W14/W16 真题解答都不是，于是这两个测试**实际还在跑旧的、已经不算正文的编造题**，新写的 10 道有代码的真题解答**一次都没被语义闸门执行过**——`81/81 通过`这个数字没有反映出来；②W14 T6（30204 小P的LLM推理加速）、W16 T6（25353 排队）**只写了一段文字描述算法，没有可运行的参考解答代码**，这两道题连"抄错了没有"都无法核实；③ 新引入的 W13 引用（"筛法的内层切片写法可回看 W04 的埃氏筛实现"）本身也有事实错误：W04 现有的 `sieve()` 是普通 for 循环写法（`for j in range(i*i, limit+1, i): is_prime[j]=False`），**并没有切片写法**——唯一的切片写法（`p[i*i::i] = bytearray(...)`）现在恰好被挪进了 W05 那段隐藏注释里，学生回看 W04 根本找不到"切片写法"。人已确认「再打回」，细节见本轮 `HANDOFF.md`/`NOTES-claude.md` 条目。✅ **`8bf7e7f`（Codex，2026-09-22）第三次交付：接受，T-028 完成。** 新增 `visible_stdin_blocks()`，只从 `<!-- 历史草稿` 之前取当前正文代码，W14/W16 的语义测试改用它（不再误抓隐藏注释里的旧代码），旧的 8 个用例改挂 `@case('ARCHIVE', ...)` 并标注"历史草稿"；W14 T6（30204）、W16 T6（25353）补上了真正能跑的参考解答（前者二分+奇偶任务分配、后者离散化+双树状数组），各配 80 组随机穷举对拍；W13 引用改成"从 `i*i` 开始逐个标记倍数的循环"——**这次我读了 W04 的 `sieve()` 源码核对过，属实**。Claude 独立复核：`verify_courseware.py` 本地重跑通过（237 代码块/281 题号，且本机装了 `python-pptx`，第 6/10 项产物层**没有跳过**，同样全绿）；`check_note_code.py` 本地重跑 **75/75 通过**，新测试 `t_w14_real_exam_solutions`/`t_w16_real_exam_solutions` 确认命中的是当前正文六段代码（不是历史草稿）。⚠️ **顺手发现并直接修了一个小问题**：`tools/test_gate.py` 有 2 条回归测试（`test_five_question_paper_fails_exam_spec`、`test_score_in_question_heading_fails_exam_spec`）的变异目标字符串还是旧标题（`'## T6. 补码计算器'`、`'## T1. 成绩转换'`），T-028 改标题后这 2 条测试直接 ERROR（不是"假绿"，是报错，但说明这条回归的覆盖面在 T-028 期间空了一段）——Claude 已改成当前标题，重跑通过，提交在本轮收尾里。`test_gate.py` 全量跑还有 **5 条无关失败**（`test_score_column_in_deck_ladder_fails_exam_spec` 等），全部指向 `courseware/content/w05.py`/`w14.py`/`w16.py` 已被 `a446437`（pptx_builder 迁移，**早于 T-028**）改成占位 stub，`git log` 确认这三个文件自 `a446437` 后再没被 T-028 的任何一次提交碰过——**与 T-028 无关，是迁移遗留的独立缺口**，已开 **T-029** 挂账，不算在 T-028 的验收范围内。Q-10 两个子问题：18 题选定不变；W14/W16 用的比赛时间顺序倒挂，Codex 已在 NOTES 里说明是有意维持现状，人未反对，视为已确认。🔴 **人指出 T-028 重新打开（2026-09-22）**："相应的 pptx 还没有换下 AI 生成讲义时编的原创模拟题的讲法，换上真正模拟考题目的相应讲法"——查证属实，且比之前三轮任何一个问题都严重：**三份课件（`.pptx`，源在 `pptx_builder/decks/*.js`）里，每道题的完整逐题讲解页（题意 / 参考解答 / 常见失分）一次都没有被三轮改动碰过**，三轮里唯一被改的只是每份课件开头那一张"难度梯度总览表"（W05 的 `3.0`、W14/W16 的"样卷总览"）。也就是说学生打开 PPT 看到的 `T1`–`T6` 逐题详解页，标题、题面、参考解答**全部还是编造的旧题**（W05: 成绩转换/单词首字母大写/图书借阅排行/区间内的 T-数/电梯调度模拟/补码计算器；W14: 课程互选统计/最优装载顺序/网格中的宝藏/分组考试/书架分层/敌友阵营；W16: 成绩单核对/括号嵌套深度/会议室数量/穿墙迷宫/作业时间分配/电网巡检），而且**不像 md 那样有"已废弃"标签或 HTML 注释隐藏**——是完全正常、无标注地呈现为当前教学内容。`w05_october_exam_review.js` 里甚至留了一条源码注释"legacy synthetic walkthrough below is retained only for audit"，说明 Codex 第一轮就知道这些页没换，但从没有把这个决定写进 PLAN/HANDOFF 让人或 Claude 看到，三轮里也没人去读课件源码的全文去核实，只看了 diff——**这是 Claude 连续三轮复核的方法漏洞**：只验证「改了什么」，没有验证「该改的地方是不是都改了」。这次要修的清单（每周 6 道，旧标题→新真题，已在下面列全，Codex 不需要重新比对）：<br>**W05**（`content("T1".."T6"` 附近，约 L208–384）：T1 成绩转换→E29895 分解因数；T2 单词首字母大写→E29940 机器猫斗恶龙；T3 图书借阅排行→M29917 牛顿迭代法；T4 区间内的 T-数（两页：concept+sieve）→M29918 求亲和数；T5 电梯调度模拟（两页：concept+code）→M29949 贪婪的哥布林；T6 补码计算器（两页：concept+code）→T29947 校门外的树又来了。<br>**W14**（约 L310–580，注意部分题还有单独的"错误归因"页）：T1 课程互选统计→E29945 神秘数字的宇宙旅行；T2 最优装载顺序→E29946 删数问题；T3 网格中的宝藏（含错误归因页）→E30091 缺德的图书馆管理员；T4 分组考试→M27371 Playfair密码；T5 书架分层→T30201 旅行售货商问题；T6 敌友阵营（含错误归因页）→T30204 小P的LLM推理加速。<br>**W16**（约 L253–370）：T1 成绩单核对→E29982 一种等价类划分问题；T2 括号嵌套深度→E30086 dance；T3 会议室数量→M25570 洋葱；T4 穿墙迷宫→M28906 数的划分；T5 作业时间分配→M29896 购物；T6 电网巡检→T25353 排队。每题的题意复述与参考解答直接照抄对应 `.md` 文件里已经写好、已通过语义闸门验证的版本即可，不需要重新构思。**顺手多发现一处**（非本轮必须修，登记进 T-029）：W16 `pptx_builder/decks/w16_review_final_machine_exam.js` 的"5 建议样卷 / 样卷总览"表格里仍列着 `分值` 列（15/15/15/15/20/20，总分 100），这是**改题前就存在的老问题**（`git log -p` 确认分值列在换成真实题号之前就有），违反 2026-09-01 决策记录"梯度表只写星级、不写分值"这条红线，但因为这张表标题是"样卷总览"不是"难度梯度"，闸门第 9 项的判据（`'难度梯度' in slide[1]`）按标题字符串精确匹配，没扫到它——是闸门覆盖面的另一个盲区，不是这轮引入的，一并记入 T-029。✅ **第四轮由人直接指定「你来完成 T-028」，Claude 自己动手改了三份 `.js`，未经 Codex**：按上面列的映射表，逐周把 `content("T1"..."T6"` 的题意/代码/样例换成对应 `.md` 里已验证过的真题内容；W05/W14 各从「多页（concept+code/错误归因）」精简成「每题一页」（题意复述 + 参考解答 + 真实样例 + 一条方法要点），不再编造评分点值或红队断言——这些在 `.md` 里也没有，编出来等于重犯本任务本来要修的错。W16 另外发现 `pptx_builder/decks/w16_review_final_machine_exam.js` 独有一段"5.7 红队测试数据"页，整段引用 `tools/redteam_exam.py` 对**旧编造题**验证过的反例——`.md` 侧这段早已被前几轮挪进 HTML 注释，但 `.js` 侧一直是可见正文，同样没人读全文发现；这次直接删除该页（不编造新数据顶替），`redteam_exam.py` 需要为新题重写红队用例，另开 **T-030** 挂账。逐张用 `node lib.js check` + `./qa.sh`（LibreOffice 渲染）+ `check_bottom.py`（页脚越界检测）验证，第一轮修了 3 处页脚越界（W05 T3、W14 T1、W16 T4，均为字号/位置微调）。⚠️ **`check_bottom.py` 全绿之后又追加了一轮人工逐页截图看**，因为它只查"正文有没有压到页脚"，不查"单个色块自己有没有装下它该装的文字"——结果又发现 3 处 `consoleBlock`（深绿色输出框）文字溢出框外（W14 T1、W14 T5、W16 T4，最后一行样例输出溢出到框外，颜色浅、肉眼一扫容易漏看）和 2 处 callout 标题过长换行挤压正文（W16 T3、T5），共追加修了 5 处，两轮合计 8 处版面问题，全部为字号/位置/文案微调，不涉及代码逻辑。页数变化：W05 31→**28**、W14 40→**32**、W16 39→**38**（合计 593→**581**），`courseware/README.md`、`pptx_builder/README.md` 已同步。闸门：`verify_courseware.py --render` 全绿（581 页 / 237 代码块 / 281 题号）；`check_note_code.py` 75/75（未改 md，数字不变）；`test_gate.py` 未受影响（T-028 相关的 2 条已在上一轮修过，T-029 的 5 条依旧、不属于本轮范围）。⚠️ **未做的事**：没有走 macOS PowerPoint 复核（本轮改动者就是 Claude 本人，没有 Codex 的 macOS 环境）——用 LibreOffice 渲染 + 页脚越界检测代替，按本仓库先例这是较弱但仍然有效的证据，建议下次有 macOS 环境时补一次 T-007 式复核 |
| T-030 | 为 W16 新的 6 道真题重写红队测试数据（`tools/redteam_exam.py` + 讲义"5.7"节），当前脚本与讲义历史注释里的内容都是针对旧编造题 | Done | Codex + Claude 复核修 T4 | T-028 第四轮（Claude 直接改 pptx）时发现：`courseware/pptx_builder/decks/w16_review_final_machine_exam.js` 曾有一整页"5.7 红队测试数据"，数据来自 `tools/redteam_exam.py`（T-009 产物），但该脚本里的 6 个 oracle/错误实现全部针对**旧编造题**（成绩单核对、括号嵌套深度……），跟当前真题（一种等价类划分问题、dance……）无关。`.md` 侧这段内容早已被前几轮挪进 HTML 注释（学生看不到），但 `.js` 侧这页一直是可见正文，本轮已删除。`handoff.py --verify` 仍会调用 `tools/redteam_exam.py`（见 `tools/handoff.py:130`），它测的是旧题、不是当前样卷，闸门显示"通过"并不代表新题已有红队验证。重做时可参照 T-009 的方法（为每题写正确 oracle + 有代表性的错误实现 + 能实际触发失败的反例数据），完成后把结果写回讲义（可以另开一节，不必强求编号 5.7）。✅ **Codex（`2b7bc30`）交付，Claude 复核后接受并修了一处**：`tools/redteam_exam.py` 六个 `test_t1`–`test_t6` 全部换成当前真题的 oracle/错法对照，W16 讲义可见正文新增"5.7 当前样卷红队反例"一节（旧数据仍只留在历史注释里）；协作记录（`HANDOFF.md`/`NOTES-codex.md`）这次都写了。独立重跑 `verify_courseware.py`、`check_note_code.py`（75/75）均通过，`python3 tools/redteam_exam.py` 本地重跑 6/6 通过。**但没有只信"6/6 通过"这个数字**——逐条读了每道题的判据逻辑，发现 **T4 的判据是假的**：`require(15 != 4, 'T4 ordered-composition mutant survived')`——两个硬编码常量比大小，不调用任何代码、不依赖任何输入，恒为真，跟 T4 的参考解答或错误实现完全没关系。这正是 T-009 原版明确要求避免的"恒真断言"反模式（当时 Claude 复核 T-009 时就修过 3 处这类问题），这次在重写时又长回来了。Claude 直接补了一个真正的 oracle/mutant 对照（`good` 按 `low` 参数递归求划分数，`bad` 去掉 `low` 下界后递归求有序组合数，二者用同一套递归结构、只差一个参数，实测 `good(7,3,1)=4`、`bad(7,3)=15`，与讲义数字一致），重跑后 `verify_courseware.py`/`check_note_code.py`/`redteam_exam.py` 均绿。其余 T1/T2/T3/T5/T6 的判据逐条读过，虽然不像 T-009 原版那样直接跑参考解答代码（是独立写的等价 oracle，不是从 `.md` 里 `run_stdin_solution` 抽取的），但每条都有真实计算、`good`/`bad` 结果确实不同，不是恒真断言，予以接受 |
| T-029 | `tools/test_gate.py` 有 5 条回归测试因 `content/w05.py`/`w14.py`/`w16.py` 已被 pptx_builder 迁移改成占位 stub 而失败，需要迁移到指向 `pptx_builder/decks/*.js` 或改用别的判据 | Todo | 待定 | 复核 T-028 第三轮（`8bf7e7f`）时顺手跑了一次 `python3 tools/test_gate.py` 全量（32 项），除 T-028 直接导致的 2 条（已由 Claude 在同一轮修掉）外，还有 **5 条独立失败**：`test_score_column_in_deck_ladder_fails_exam_spec`、`test_two_hour_wording_in_deck_fails_exam_spec`、`test_pure_ascii_column_is_not_flagged`、`test_fullwidth_space_bullet_fails_markup`、`test_ascii_column_crossing_chinese_fails_alignment`，全部是在 `content/w05.py` / `w14.py` / `w16.py` 里找变异目标字符串时 `ValueError: substring not found` / `AssertionError`——`git log` 确认这三个文件最后一次改动是 `a446437`（"Courseware W04-W16: rebuild with pptx_builder"），此后再没人碰过，现在的内容只是"课件改由 pptx_builder 生成…这里只保留 META"的占位说明。也就是说 `a446437` 把 W04–W16 的课件源迁到了 `pptx_builder/decks/*.js`，但 `test_gate.py` 里对应这几周的回归测试没有跟着迁移，一直是静默失效状态（不影响 `verify_courseware.py`/`check_note_code.py` 的日常验收，只影响这条更深的"闸门自身失败路径"回归，即 T-010 建立的那套）。不算 T-028 的验收范围，单开一条。追加（同日）：复核 T-028 第四轮时发现闸门第 9 项还有一个覆盖盲区——`'难度梯度' in slide[1]` 是按标题字符串精确匹配，W16 `pptx_builder/decks/w16_review_final_machine_exam.js` 里标题为"样卷总览"的表格照样列着 `分值` 列（15/15/15/15/20/20），`git log -p` 确认这列在换成真实题号之前就存在，不是本轮引入的老问题，闸门没扫到是因为标题不叫"难度梯度"。顺手一并记在这条里，修的时候可以把判据从"标题精确含'难度梯度'"放宽成"任何列出`分值`/`分数`字样的表格" |

---

## ✅ 给 Codex 的硬要求（T-007）—— **已完成，原文保留备查**

> **结论（2026-08-31，`4a5d535`）**：Codex 以 **Microsoft PowerPoint 16.112.2** 通过 AppleScript
> 导出 16 份 PDF 并逐页复看 **439 / 439 页**，未发现缺字、方框、重叠、裁切、错位、意外空白
> 或非预期标记泄漏。**Office 自动化可用，因此不存在需要保留的阻塞。**
> 下面的原始要求保留不删，供后续学期复用同一流程。


**Codex 在 macOS 上运行，装有 Microsoft PowerPoint。对最终交付而言，
PowerPoint 是比 LibreOffice 更合适的渲染器 —— 学生和教师放映用的就是它。**

因此本轮的课件复核**必须**按下面的顺序做：

1. **用 PowerPoint 把 `courseware/*.pptx` 导出为 PDF**（Office 自动化，如 AppleScript /
   `osascript` 驱动 `Microsoft PowerPoint` 的 `save as PDF`）；
2. **按该 PDF 输出做完整的缩略图复核**：439 页逐页看，检查缺字、方框、重叠、裁切、
   错位、意外空白、`**` / 反引号等标记泄漏；
3. **在 HANDOFF 里写明使用的 PowerPoint 版本号与实际复看页数**。

**若 Office 自动化不可用**（未授权、脚本被拒、沙箱限制等），
**如实保留该阻塞并写清失败原因**，
**不要**把 LibreOffice 的字体替代结果当作最终结论 ——
LibreOffice 缺少「微软雅黑 / Consolas」时会做字体替换，
它的"版面没有越界"不等于 PowerPoint 下的成品可读、不等于版面一致。

> 闸门第 8 项（`--render`）用的就是 LibreOffice。它的定位是**回归检测**
> （"这次改动有没有把版面撑破"），**不是交付验收**。两者不能互相替代。

---

## 未决 / 待人拍板

| # | 问题 | 现状 | 谁能拍板 |
| --- | --- | --- | --- |
| Q-1 | **周次 → 月份的映射是推算的** | 文件名 `YYYYMM` 按「第 1 周 ≈ 9 月上旬」推算：W01–05→09、W06–09→10、W10–13→11、W14–16→12。2025 秋第 1 周是 9 月 9 日；2026 秋若开学日期不同，需批量重命名 | 人（需要校历） |
| ~~Q-2~~ | ~~**OJ 题号只做了离线比对，没有联网确证**~~ **已销账** | **T-008 联网核实 56 OJ + 19 LC 端点，编号与链接全部正确**，并查出 1 处过时题名（12559 的 `v0.3` 后缀）。原顾虑成立且被证实 —— **离线语料确实会过时**。后续保障：闸门新增 `VERIFIED_TITLES` 覆盖层，已核实题号以平台题名为准、语料被忽略，防止过时写法回潮（变异自检已验证会变红） | 已关闭；出处 `b2e6e3e` + Claude 第 3 轮复核 |
| Q-3 | **样卷难度分布与 AC 率是估计值** | T1 90% → T6 15% 的预期 AC 率是命题经验估计，没有历史数据支撑 | 人（有往年数据后校准） |
| ~~Q-4~~ | ~~**机考的确切时长与题量**~~ **已销账** | **人给出事实：每次机考（三次月考 + 期末机考）都是 112 分钟、6 题。**原先讲义写「约 120 分钟」，且 W05 样卷只有 5 题、W14 只有 4 题 —— 三处都与事实不符。已按 T-012 全面对齐，并新增闸门第 9 项把这条规格钉死（此前它只靠人眼守）| 已关闭；人拍板 + `T-012` |
| Q-5 | **成绩权重未定** | 讲义只写"以当学期正式教学通知为准"，并给了 2025 年的 AC 数 ↔ 优秀的经验对应，**没有预设任何未公布的比例** | 人 |
| Q-6 | **上机实习课 32 学时的具体安排未细化** | 讲义在 W02 / W13 / W15 给了三次成型的上机任务，其余周只有作业题单；32 学时如何分配未核 | 人 |
| ~~Q-7~~ | ~~**`deck.py` 的代码页在行数过多时会静默溢出**~~ **已销账** | **量过之后发现不是理论风险**：cs101 有 **4 张已钳位**（最紧 7.01pt），其中 **3 张实测把代码压在页脚上**；cs201cq **0 张**、最紧 9.26pt —— 数据表明加硬报错对 cs201cq 零影响，跨仓库顾虑就此消解。已办：`deck.py` 触底即 `raise LayoutOverflow`（两仓库同步，字节一致）、拆掉那 4 张页、并修好渲染检查的判据（详见 Q-8） | 已关闭；人拍板「三项都做，两个仓库同步改」 |
| ~~Q-8~~ | ~~**渲染检查比对的是「页面」边界，不是「版心」边界**~~ **已销账** | 查 Q-7 时发现的更严重问题：那 3 张坏页压住页脚仍在页内，**第 8 项照报「0 处越界」**，Codex 的 PowerPoint 逐页复核也没看出来。判据已改为版心底部（485.3pt），阈值按**实测**页脚文本顶端 497.5pt 校准（先按理论值 498.2 试过，差 0.7pt 就把每一页页脚全判成溢出）。变异自检：守卫失效 + 合回 36 行 -> 新判据命中、旧判据零命中 | 已关闭；本轮 |
| ~~Q-9~~ | ~~**`deck.py` 已与 cs201cq 分叉**~~ **已销账** | 人已在终端同步并提交（cs201cq `3dd9675`）。Claude 复核：两仓库 `deck.py` **恢复字节一致**（sha256 `208d6a36cdb730d9`）。⚠️ 我开这条时只量了"粗体套反引号"这一种触发方式，**漏了刚发现的第二种（粗体内容含 `*`）** —— 已补量：cs201cq 两种都是 0 处；更要紧的是直接验产物，**用新引擎重建它的 16 份课件，可见文本逐份一致**，同步确证是零影响 | 已关闭；人同步 + Claude 复核 |
| ~~Q-10~~ | ~~**T-028 换题该换成哪几道，换多少**~~ **已销账** | 18 题选定：3 场真实比赛、18 道题，与 W05/W14/W16 一一对应（`20251009mockexam`→W05、`20251204mockexam`→W14、`20251106mockexam`→W16）。旧编造题的去向：第三轮（`8bf7e7f`）确定为挪进 `<!-- -->` HTML 注释、不再是可见正文，人未反对，视为拍板。W14/W16 比赛时间顺序倒挂：Codex 说明是有意维持现状（题目入口是既定比赛，不擅自对调），人未反对 | 已关闭；T-028 第三轮验收时一并确认 |

---

## Decision Log

| 日期 | 决策 | 理由 | 出处 |
| --- | --- | --- | --- |
| 2026-08-31 | 讲义与课件**同名成对**放在 `courseware/` | 沿用 cs201cq 的既有约定；二者是一套材料，分开放会漂移 | Claude；本轮 |
| 2026-08-31 | 课件**由脚本生成**，不手工排版 | 便于批量改样式、逐年复用；也让"页数/版面"可被闸门验证 | Claude；本轮 |
| 2026-08-31 | 直接复用 cs201cq 的 `deck.py`，一行不改 | 两门课的课件样式应当一致；引擎有改动时两边同步，避免各自演化 | Claude；本轮 |
| 2026-08-31 | 课程指南的「课程安排」表作为教学内容的**唯一事实源** | 讲义头部与课件 META 逐字复述，闸门第 3 项直接解析该表比对 | Claude；本轮 |
| 2026-08-31 | 第 15、16 周共用课程安排表的「第 15-16 周」一行，各加一条 `本周侧重` | 表里这两周是合并的一行，但可交付物是两周；逐字复述那一行 + 单独说明侧重，闸门仍能逐字比对 | Claude；本轮 |
| 2026-08-31 | 题号↔题名核对做成**离线**闸门（第 7 项） | 本机无外网。用仓库既有材料当语料可覆盖 73 处；**并在输出里如实报告"无从判定"的处数**，不假装它等于联网核实 | Claude；本轮 |
| 2026-08-31 | 讲义里凡带注释输出的代码，注释必须是**实跑结果** | 本轮共修正 5 处：W06 保护圈输出、W11 分数背包 19.0→20.0、W14 三处（余弦值、注意力权重、样例答案 11→6） | Claude；本轮 |
| 2026-08-31 | 语义验证做成**可重复脚本**而非一次性抽查 | 一次性结论只留在 NOTES 里，下一轮无法重跑；进闸门才有约束力 | Claude；`check_note_code.py` |
| 2026-08-31 | **交付验收用 PowerPoint，回归检测用 LibreOffice** | 放映机器上跑的是 PowerPoint；LibreOffice 的字体替代会掩盖真实版面问题。两者定位不同，不可互相替代 | 人指示；T-007 |
| 2026-08-31 | 闸门抓到的问题，修**内容**而不是放宽**检查** | W15 的 XOR 代码页溢出 → 拆成两页（而不是调小字号下限）；W10 的退化区间 → 在讲义里写明题目前提（而不是删掉测试） | Claude；本轮 |
| 2026-08-31 | **T-007 验收通过：课件以 PowerPoint 成品为准已合格** | PowerPoint 16.112.2 逐页复看 439 页无缺陷。Claude 侧能独立核验的三项（逐周页数三方一致、提交未触及课件源、60 张浅灰页均为有标题的 section 页）全部成立；**视觉判断本身无法在 Linux 侧复现，这正是该任务交给 Codex 的原因** | Codex 执行 / Claude 复核；`4a5d535` |
| 2026-08-31 | 页数一致性可作为"渲染的是当前版本"的**弱证据** | Codex 报的 W15=24 是 XOR 页拆分**之后**的值；若渲染的是旧文件会报 23。逐周 16 个数字全中，基本排除了陈旧或臆造 | Claude；本轮 |
| 2026-08-31 | **离线语料不是权威，联网核实才是** | T-008 实测：2025 材料把 12559 写成「最大最小整数 v0.3」，平台上没有该后缀。语料错了，闸门就会**祝福错误答案** | Codex；`b2e6e3e` |
| 2026-08-31 | 闸门第 7 项**去掉子串容差**，改精确匹配 + 更干净的归一化 | 旧规则里 `最大最小整数` 与 `最大最小整数v0.3` 互为子串，漂移被静默放过。归一化剥掉 Markdown 链接残留后，精确匹配对 263 处引用**零误报** | Claude；第 3 轮 |
| 2026-08-31 | 新增 `VERIFIED_TITLES`：已核实题号**以平台题名为准，忽略语料** | 只收紧匹配规则并不够 —— 变异自检显示，改回 `v0.3` 时收紧后的规则**依然全绿**，因为语料里就有这个错写法。必须让核实结果压过语料 | Claude；第 3 轮 |
| 2026-08-31 | 闸门第 7 项补上**表格形态**引用（`\| 题名 \| 题号 \|`） | 变异自检暴露：W16 备选题库的 71 处引用**从未被检查过**，那里恰是全仓库题号最密集处。覆盖 73 → 263 处 | Claude；第 3 轮 |
| 2026-08-31 | 表格解析**按表头列名定位**，不用"紧邻前一格"启发式 | 启发式在 `\| 形状 \| 排序键 \| 例题 \|` 这类表上把排序键当题名，实测 5 处误报 | Claude；第 3 轮 |
| 2026-08-31 | 题名正则**允许括号**，改在 `]` 处截断 | 旧写法在 `(` 处截断，把语料里「汉诺塔问题(Tower of Hanoi)」截成「汉诺塔问题」，反把讲义正确全称判成不符（3 处假阳） | Claude；第 3 轮 |
| 2026-08-31 | 红队"证据"必须**依赖 fixture**，不能是恒真的常量比较 | `cfc7c94` 里有 2 处 `require(1e9 > 1e8)` / `require(n*(n-1)//2 > 4e9)` —— 脱离 fixture 单独跑也通过，证明不了任何事。已改为从 fixture 推导（T3）和**实测步数**（T6） | Claude；第 4 轮 |
| 2026-08-31 | 红队的"错误实现"必须是**忠实的错法**，不能硬编码到某组输入 | `t6_bad_shortest_sum` 原以 `return 6 if distance[n] == 6 else -1` 收尾，换一组输入就退化成 -1，并非真的"最小和路径上的最大边"。已改为真实现，并补一组瓶颈 10 / 最小和路径最大边 11 的对照输入 | Claude；第 4 轮 |
| 2026-08-31 | **红队 fixture 必须去跑讲义自己的参考解答**，而不只是脚本内的 oracle | 原实现里 oracle 是另写的一份，六段"给学生照抄"的代码**一次都没被 fixture 碰到**（W16 当时只有 1 项语义测试）。已接入：18 组输入全部比对讲义承诺的输出 | Claude；第 4 轮 |
| 2026-08-31 | 讲义里"易错点"的措辞也要能被测试证伪 | 变异自检发现 T5 的易错点写成"`cand >= dp[c]` 会在得分更低时也覆盖"，实际该替换**与原式逻辑等价**；真正的错法是**少写一支**。措辞已改，并注明等价性 | Claude；第 4 轮 |
| 2026-08-31 | 红队套件并入 `handoff.py --verify` | 一次性跑过的结论留在 NOTES 里下一轮无法重跑；进闸门才有约束力（与 `check_note_code` 同理） | Claude；第 4 轮 |
| 2026-08-31 | **跨仓库改动的顾虑，用数据消解，不靠讨论** | Q-7 悬了两轮。复算两仓库全部 380 张 code/ascii 页后结论是"cs201cq 零触发"，改动风险从"未知"变成"已知为零"，人当场就能拍板 | Claude；第 5 轮 |
| 2026-08-31 | 能在**构建期**精确算出来的量，就不要留给渲染期去碰运气 | 字号是 deck.py 自己算的，触底时它最清楚。原先钳位后静默继续，把发现问题的责任推给 4 分钟的渲染检查，而渲染检查当时还查错了对象 | Claude；第 5 轮 |
| 2026-08-31 | **检查的"对象"错了，比检查"不够严"更危险** | 第 8 项一直在比页面边界（540pt），而版心底部是 485.3pt。中间那 55pt 是页脚区 —— 正文压进去照样"全绿"。3 张坏页因此躲过了渲染检查 **和** PowerPoint 逐页复核两道关 | Claude；第 5 轮 |
| 2026-08-31 | 版面阈值一律按**实测**取，不按理论值 | 页脚文本框在 498.2pt，但 LibreOffice 渲染出的文字 yMin=497.5。按理论值设阈值，443 页里几乎每页的页脚都被误判成溢出 | Claude；第 5 轮 |
| 2026-08-31 | 回归套件必须有**反向对照**（干净输入零失败） | Codex 的 7 项只断言"退出码非零 + 出现某检查名"。若副本本身坏了，闸门对任何输入都非零，**7 项会全部"通过"**。已补一条干净副本必须退出 0 | Claude；第 6 轮 |
| 2026-08-31 | 回归用例要**覆盖到分支**，不能只覆盖到检查项 | 两条题名用例都打在 12559 上，而它在 `VERIFIED_TITLES` 里走覆盖层分支 —— 「语料 + 精确匹配」那条路径一次没执行。实测：把子串容差加回去，两条用例依然全绿。已补一条走语料分支的（01017「装箱问题」→「装箱」） | Claude；第 6 轮 |
| 2026-08-31 | 回归套件本身也要做**变异自检** | "测试通过"不等于"测试能发现问题"。逐条回退闸门修复（覆盖层 / 表格解析 / 子串容差 / 大纲 / 渲染阈值 / deck 守卫 / 可重生成），确认 **7/7 变红**，才算这套回归有效 | Claude；第 6 轮 |
| 2026-08-31 | 慢判据抽成**具名函数**以便脱离外部工具回归 | 第 8 项要跑 LibreOffice（4 分钟），没法进单元测试。把判据抽成 `intrudes_into_footer()` 后，用实测坐标直接回归那个 0.7pt 的校准 | Claude；第 6 轮 |
| 2026-08-31 | 第 6 项必须比对**重建产物与已提交课件的文本**，不能只比页数 | Codex 审查（P1）：改 `content/wNN.py` 的标题、保留旧 `.pptx`，第 1–7 项全绿 —— 红线 2「pptx 是产物」有个反面从没被检查：**源改了忘了重建**。改标题时页数往往不变，只比页数必然漏 | Codex 发现 / Claude 落地；第 7 轮 |
| 2026-08-31 | 不比对 XML / 字节，比对**可见文本** | zip 时间戳与元素顺序每次生成都变，字节比对必然假阳；可见文本是唯一稳定的等价判据（此前人工复核 Codex 的 W06 改动时用的就是它，只是没进闸门） | Claude；第 7 轮 |
| 2026-08-31 | 回归测试必须**剔除** `VERIFY_RENDER` 等外部环境变量 | Codex 实测：`VERIFY_RENDER=1 handoff.py --verify` 时，`test_gate` 的每个临时副本都会去调 LibreOffice，结果随本机字体环境浮动。失败路径判定与渲染无关，不该被它左右 | Codex 发现 / Claude 落地；第 7 轮 |
| 2026-08-31 | 历史性验收记录**不随后续改动改写**，而是标注"其后又改了什么" | T-007 复核的确实是 439 页版本。把它改成 443 是篡改历史，且会掩盖"新拆的 8 张页从未经 PowerPoint 看过"这一真实缺口。已保留原数并新开 T-011 | Claude；第 7 轮 |
| 2026-08-31 | 增量验收只复核**变动的那几页**，不重跑全量 | T-007 已覆盖 439 页，Q-7 只新增/改动 8 页。让 Codex 重看 443 页是浪费；只看 8 页即可把覆盖补齐。前提是"哪几页变了"必须能从 `content/` 精确算出来 | Claude；第 7 轮 / Codex 执行 `5c63737` |
| 2026-08-31 | 页码位移可作为"渲染的是当前版本"的**强证据** | Codex 报的 W16 页码 20/21/23/24/28/29 是**前序拆分后位移的结果**，不是连续段。六个非平凡页码全中，比页数一致更难碰巧对上 | Claude；第 8 轮 |
| 2026-09-01 | 三次月考与期末机考按**同一规格**成文：6 题 / 112 分钟 / 15+15+15+15+20+20 | 人给出的事实只有"112 分钟、6 题"，但月考样卷此前是 5 题 / 4 题、分值各不相同。统一规格才使"月考是同构演练"这句话成立 —— 否则学生按月考的题量估时间，到机考会崩 | 人指示 / Claude 落地；第 9 轮 |
| 2026-09-01 | 这条规格**不在课程指南表里**，因此另立闸门第 9 项，而不是挂在第 3 项上 | 第 3 项的事实源是课程指南的「课程安排」表，那里没有时长与题量。人给的口径同样是事实源，只是另一个 —— 混进第 3 项会让"逐字复述指南"这条判据失真 | Claude；第 9 轮 |
| 2026-09-01 | 补题一律**追加**，不重新编号 | W13 有一处"第 5 周月考样卷 T4 的筛法"的交叉引用。把新题插进中间会静默改变所有既有编号引用 —— 红线「题号必须对齐」在这里是活的。代价是新题必须自己够难（W05 T6 定为 ★★★★☆）| Claude；第 9 轮 |
| 2026-09-01 | W05 的新题选**补码 / 位运算**，因为那是全卷唯一"自检清单里有、样卷里没有"的知识点 | 原 5 题覆盖 W1、W2、W4，整个 W3（计算机原理 1）没有一道题。补题的第一判据是**补覆盖缺口**，不是"再加一道难题" | Claude；第 9 轮 |
| 2026-09-01 | 「错误归因」表里的每一行都要有**反例**，否则只是口气重的猜测 | W14 讲评的主体就是这些表。已为两条可判定的 WA 归因各造反例（`== k` 判据、漏写对称合并），并在用例里断言"错法与正解**必须不同**"——fixture 选错了会当场失败。TLE 那几行靠计时不稳定，讲义里已写明是复杂度推算而非实测 | Claude；第 9 轮 |
| 2026-09-01 | 同一族缺陷有几种触发方式，**量的时候必须都量到** | 开 Q-9 时我只扫了"粗体套反引号"，漏了**同一轮里刚发现的**第二种（粗体内容含 `*`）。若 cs201cq 恰好有这种写法，同步引擎会让它的已提交课件**静默变陈旧**。已补量，两种都是 0 处 | Claude；第 11 轮 |
| 2026-09-01 | 跨仓库影响的**决定性证据是重建产物，不是数源里的写法** | 数写法要求我把触发方式列全 —— 而我刚证明过自己列不全。改成"用新引擎重建 cs201cq 的 16 份课件、比对可见文本"，逐份一致，这条结论不依赖我对触发方式的枚举是否完整 | Claude；第 11 轮 |
| 2026-09-01 | 两条**独立路径**推出同一组页码，比任一条单独可信 | 那 6 页：Codex 走"`c64ad70^` 与当前 PPTX 的可见文本逐页 diff"，我走"从 `HEAD~2` 旧源按旧引擎语义反推"。两条路的假设几乎不重叠，结果完全一致 | Claude；第 11 轮 |
| 2026-09-01 | **人眼逐页复核抓不住"小记号"，这类不变量必须由代码守** | 一个反引号，在 T-007（439 页）、T-011（8 页）、T-013（29 页）三轮 PowerPoint 逐页复核里**全部漏过**，共 9 处。不是谁不认真——是这个方法对这个尺度的缺陷本来就不灵敏。已做成闸门第 10 项 | Claude；第 10 轮 |
| 2026-09-01 | 记号泄漏检查的对象是**产物里 run 的字体**，不是源的写法 | 源里的错写法千变万化（粗体套反引号、粗体内容含 `*`、将来还会有别的），泄漏的**表现只有一种**：非等宽 run 里出现了 `**` 或反引号。按表现查，才不会写一条规则漏一条 | Claude；第 10 轮 |
| 2026-09-01 | 闸门抓出的第三处，比人报的两处更能说明问题 | W02 p27 的 ``**`[[0]*n]*m` 别名陷阱**`` 是**新检查上线后当场报出来的**，我和 Codex 都没看见它。一条检查的价值不在于复现已知缺陷，而在于它还能报出你没找的那些 | Claude；第 10 轮 |
| 2026-09-01 | 动共用引擎之前，先量另一个仓库 —— 与 Q-7 同一套动作 | cs201cq 的 16 个模块里该写法 0 处，所以这次修复对它零影响。**量完再说，风险就从"未知"变成"已知为零"** | Claude；第 10 轮 |
| 2026-09-01 | 工具的行为**不该取决于谁在什么上下文里调它** | `check_note_code.py` 会真的执行讲义里 `data = sys.stdin.read().split()` 这类模块级赋值。调用方 stdin 若是一条常开管道，套件**永久阻塞、零输出**，看上去只是"跑得慢"——本轮为此浪费了两次 5 分钟。已隔离 stdin，并补一条用 `os.pipe()` 复现该场景的回归（撤掉修复即 120s 超时变红）| Claude；第 9 轮 |
| 2026-09-01 | 同一个事实只留**一个出处**：分数写在样卷题头与分值分配行，难度梯度表只写星级 | 分值此前有四处写法（题头、分值分配行、课件 bullet、梯度表第二列）。人明确「机考之后另有成绩核算办法」，梯度表里的分数既冗余、又会跟真正的核算口径打架。梯度表本来就只该回答"多难"，不回答"几分" | 人指示 / Claude 落地；第 12 轮 |
| 2026-09-01 | 顺手修的**必须是同一处**、且必须说明它早于本轮 | W05 梯度表 T3/T4 的 1 格错位在 `HEAD~2` 即存在，上一轮量到过但没动。这轮既然整块重排这张表，不修反而要留一个已知错位；但它不是本轮引入的，记录里要写清楚 | Claude；第 12 轮 |
| 2026-09-01 | ASCII 表的列位置用**显示宽度算出来**，不靠数空格 | 这张表混排中英文，`--` 列此前就是靠人数空格对齐才错了一格。改成按 East-Asian width 计算补白后重排，6 行的星列（第 8 格）与 `--` 列（第 49 / 48 格）严格一致 | Claude；第 12 轮 |
| 2026-09-02 | 对不齐的原因是**几何**，不是实现 —— 所以换排版手段，不是把填充算得更准 | 汉字字身宽 1 em，Consolas 约 0.55 em；两个空格 = 1.10 em，永远补不出一个汉字。我上一轮改成「按 East-Asian width 算补白」，其实只是把同一个错误算得更整齐了：源里的显示宽度一致，纸上的字形宽度依然不一致。改用表格后排版交给 PowerPoint 的单元格，不再依赖任何字宽模型 | Codex 实测发现 / Claude 定因并落地；第 12 轮 |
| 2026-09-02 | **「通过」的那一页比「未通过」的那一页更危险** | W16 p13 只有 2 行、前缀汉字数只差 2，实测极差 1.02 pt，人眼判「对齐正常」—— 它不是对的，只是漂得还看不出来。判据因此不看「漂了多少 pt」，只看「这一列是不是跨过了不同的中文字数」：前者要渲染才量得到、还会被字体替换带偏，后者在源里就是确定事实 | Claude；第 12 轮 |
| 2026-09-02 | 挂账要**明写**，不能靠删判据来装绿 | 同类还有 7 张（方框里写中文、右边框对不齐），实测 0.1–13.4 pt。删掉判据就「全绿」，但那是骗自己。写进 `KNOWN_RAGGED` 并开 T-018：新写的 ascii 图立刻变红，欠着的那 7 张也一直挂在闸门输出里 | Claude；第 12 轮 |
| 2026-09-02 | 挂账名单按**（模块, 标题）**记，不按页序号记 | 序号会被「在前面插一页」整体推后，那样豁免会悄悄挪到别的图上，真正欠着的那张反而变红。标题在模块内唯一，且插页不影响它 | Claude；第 12 轮 |
| 2026-09-02 | 换了排版手段，就要回头检查**旧判据还认不认得出问题** | 梯度表从 `ascii` 改成 `table` 之后，第 9 项原来只扫 ascii 正文 —— 表格单元格里再写一列分值会**静默放过**。这是修一个缺陷时自己挖的坑，当轮补掉（改成按页标题认、摊平所有字符串），那条变异测试也改成打表格 | Claude；第 12 轮 |
| 2026-09-02 | 复核成品时**直接读 `.pptx` 的结构**，不只读对方的描述 | 「四列表格列对齐稳定」这句话，可以是原生表格，也可以是补空格补得刚好看不出来。`shape.has_table` 为真、7×4、6 行星级逐格齐全 —— 这是结构事实，不依赖谁的眼睛。再配上页数 28 / 40 与本地重建一致，就能确定复看的是当前版本 | Claude；第 13 轮 |
| 2026-09-02 | 已知缺陷可以**明写着欠**，但欠的理由必须是幅度，不是嫌麻烦 | T-018 那 7 张实测 0.1–13.4pt，判不通过的那张是 36.85pt —— 相差一个量级，这是个能说出口的理由。同时闸门每次都报「7 张挂账」，新写的图立刻变红：**欠着 ≠ 忘了，也 ≠ 会继续变多**。触发条件也写死了：下次动到那几周的课件就顺手修 | 人拍板 / Claude 记录；第 13 轮 |
| 2026-09-02 | 概念页要给**可操作的动作**，不能只给定义 | 原来 W06 关于交换论证只有一句"交换它们不会变差"。这句是**判据**，不是**方法**：学生拿着它没法在考场上推出排序键。补的这一节把它倒过来写成「设相邻两项、写两种代价、令前者不劣、化简得 key」，并明写"推完必须对拍"——推导会错，对拍不会 | 人指定 / Claude 落地；第 14 轮 |
| 2026-09-02 | 新写的思考题**不能和后面的样卷撞题** | 我起初给的思考题是「n 个任务带 (w, t)，最小化 Σ w·完成时刻」——那正是 W14 样卷 T2（讲义自称"本卷区分度最高的一题"）。在第 6 周把它连解法一起给出去，那道题就废了。换成"01017 的『从大到小放』能用这套骨架证明吗？卡在哪一步"——考的是**方法的边界**，且不与任何样卷重合 | Claude；第 14 轮 |
| 2026-09-02 | 讲义里的 `assert` **不等于跑过了** | `check_note_code.load_week()` 只保留 def / import / 赋值，循环与裸 `assert` 会被 `_keep()` 滤掉 —— 也就是说讲义里那段对拍循环，语义套件根本没执行。所以新代码块必须**另外**配一条具名用例，并且我把整块原样抽出来当脚本跑了一遍（退出码 0，打印值与注释逐行对上）| Claude；第 14 轮 |
| 2026-09-02 | 讲义里**依赖运行时行为**的结论，必须写明测出它的环境 | W08 §2.2 原文「递归深度到 10⁵ 以上时某些评测环境会直接段错误」在 3.12 上不成立 ——纯递归 30 万层照跑，而真正会挡住学生的 `lru_cache` 场景 3331 层就断，两件事原文都没说。新表逐格标注「CPython 3.12.3 / Linux 实测」，并明写"请当成某个环境下的实测值，不是语言规范"——**这恰恰是不该依赖它的理由** | 人提问 / Claude 实测并落地；第 15 轮 |
| 2026-09-02 | 断言要卡**相对关系**，不卡实测数字 | 新用例若直接断言「3331」，换台机器就必然假红。改成断言「纯 Python 能到的深度 ≥ 10 倍于穿过 C 的深度」，并对 ≤3.10 按版本跳过（那时两墙本是一道）。数字进讲义（有环境标注），关系进闸门 | Claude；第 15 轮 |
| 2026-09-02 | 改了行为就要给**新行为**一条线，别拿旧行为的线充数 | `~$*.pptx` 豁免配的测试是「普通孤儿必须红」—— 那守的是旧行为，把豁免整段删掉它照样绿（实测）。反向对照必不可少，但它不能替代**新行为自己的绿线**。已补：副本里放一个锁文件，闸门必须仍绿；撤掉豁免则当场红 | Codex 修复 / Claude 复核补测；第 16 轮 |
| 2026-09-02 | 前置知识不够的反例，等于没有反例 | W10 §2.2 拿"最长路（有环时不成立）"当最优子结构的反例，但**图要到 W12 才讲**，第 10 周的学生只能把它当口诀背。换成 4 行数字三角形 + 奇偶约束：8 条路径能逐条列出来，朴素 DP 错在哪一格也能指出来。原来那个反例降为 W12 的回看，那时才有说清它的语言 | 人提问 / Claude 落地；第 17 轮 |
| 2026-09-02 | 写新页时我自己踩了第 11 项的漏 —— 于是把漏补上 | W10 那张三角形图我写成 `6-7-0-4 = 17 奇`，一个汉字夹在两列中间，右边那一列当场歪掉；而当时 `ALIGN_TOKENS` 里没有 `=`，**闸门一声没吭**。把 `=` `+` `#` 补进记号表后全仓库零新增（`+` 那一处落在 w01，本来就在 `KNOWN_RAGGED` 上），并补了一条变异测试。**判据的记号表本身也会漏，补漏的时机就是自己踩到的那一次** | Claude；第 17 轮 |
| 2026-09-02 | 验收版面，**图形会话里看**比导 PDF 更硬 | T-025 Codex 改成在 macOS 的 PowerPoint 图形界面里逐页截图复核。导出 PDF 会经过一次转换，而学生和老师看到的就是图形界面里的那一帧。此前几轮走的是导出 PDF —— 那些结论仍然有效，但今后优先用图形会话 | Codex 改进 / Claude 记录；第 18 轮 |
