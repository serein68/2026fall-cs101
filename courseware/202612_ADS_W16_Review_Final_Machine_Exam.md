# 第16周 课程知识体系总结、期末复习与上机考试

*Updated 2026-08-31 GMT+8*
 *Compiled by Hongfei Yan (2026 Fall)*
https://github.com/GMyhf/2026fall-cs101

> **课程安排对应**：第 15-16 周
> **主题与学习重点**：知识图谱、神经网络等 AI 专题；课程知识体系总结与期末复习。
> **本周侧重**：课程知识体系总结、期末复习与**期末上机考试**（命题方案与建议考查内容）。

**本文件的读者分两类**：

- **学生可用部分**：第 1 节知识体系、第 2 节考试须知、第 3 节能力要求、第 7 节备考建议；
- **教师用部分**：第 4 节知识点覆盖矩阵、第 5 节建议样卷（6 题，含题面 / 样例 / 参考解答 / 数据构造建议 / 评分标准）、第 6 节备选题库、第 8 节命题与阅卷检查清单。

---

# 1 课程知识体系总结

## 1.1 一张图看完 16 周

```
                        《计算概论（B）》知识体系
                                  │
        ┌─────────────────┬───────┴────────┬──────────────────┐
        │                 │                │                  │
   计算机基础          语言与工具        算法与数据结构        AI 素养
   (W1,W3,W13)         (W1,W2,W4)      (W6-W12,W16)        (W1,W14,W15)
        │                 │                │                  │
   ├ 图灵机           ├ Python 语法    ├ 复杂度分析        ├ LLM 原理
   ├ 冯·诺依曼        ├ 容器与代价     ├ 矩阵 / 前缀和     ├ 幻觉与验证
   ├ 二进制/补码      ├ Shell & VM     ├ 排序 / 二分       ├ 提示词
   ├ 浮点 & ASCII     ├ IDE & 调试     ├ 贪心 / 区间       ├ 知识图谱
   ├ 进程 & 虚拟内存  └ 快速 IO        ├ 栈 / 队列         ├ 神经网络
   └ 缓存 & 局部性                     ├ 递归 / 回溯       └ RAG
                                       ├ 并查集
                                       ├ 动态规划
                                       └ BFS / Dijkstra
```

## 1.2 算法主线：一条演化路径

```
   暴力枚举 ──剪枝──► 回溯 (W9)
       │
       ├──排序后一遍扫──► 贪心 (W6, W10)
       │                     │
       │                 贪心失效
       │                     ▼
       ├──记忆化──► 动态规划 (W10, W11)
       │
       └──按层扩展──► BFS (W12) ──边权不同──► Dijkstra (W12)

   横向工具：前缀和 / 差分 (W6,W10)、二分答案 (W12)、
             单调栈 / 单调队列 (W7)、并查集 (W9)
```

## 1.3 每周一句话

| 周 | 一句话 |
| -- | ---- |
| 1 | 三个平台各司其职；AI 是加速器不是替身 |
| 2 | `python3 a.py < in.txt` 是本课最常用的一条命令 |
| 3 | 补码让减法变加法；浮点永远不要用 `==` 比较 |
| 4 | 容器复杂度表要背；先看数据范围再定算法 |
| 5 | 月考是体检；订正 = 分类 + 关题解重写 |
| 6 | 贪心 = 排序 + 一遍扫，难在选排序键 |
| 7 | 栈用 list、队列用 deque；单调结构把 O(n²) 降到 O(n) |
| 8 | 递归三法则；递归就是在用系统栈 |
| 9 | 回溯 = DFS + 撤销；并查集摊还近 O(1) |
| 10 | 要"多"按右端点排，要"合/盖"按左端点排；DP 三要素 |
| 11 | 0-1 倒序、完全正序；"恰好装满"要用 ±inf 初始化 |
| 12 | 用 deque、入队标记；要"最短"就 BFS |
| 13 | Python 慢就换算法，不要抠常数 |
| 14 | 幻觉源于"训练目标是合理而非正确" |
| 15 | 反向传播本质上就是动态规划 |
| 16 | 考场流程：看范围 → 认题型 → 定策略 → 造数据 → 查格式 |

---

# 2 考试须知

| 项目 | 规定 |
| ---- | ---- |
| 时间 | 第 16 周（具体日期、节次以通知为准） |
| 时长 | **112 分钟** |
| 题量 | **6 道**编程题 |
| 总分 | 100 分（分值见第 5 节） |
| 地点 | 机房（座位与分配以通知为准） |
| 平台 | OpenJudge（cs101 小组） |
| 语言 | **Python 3 为主，允许 C++** |
| 环境 | 考试专用机房，**断开外网** |
| 允许携带 | **一页 A4 双面手写 cheat sheet**（不得打印、不得夹带整页代码） |
| **禁止** | ⚠️ **任何 AI 工具**（含本地模型、IDE 智能补全插件）、任何联网查询、任何形式的交流 |
| 学术诚信 | ⚠️ **无法解释自己提交的代码，按学术不端处理**，成绩记 0 |

**判分方式**：以 OJ 判定的 AC 情况为准，按题给分；部分题设分档数据（部分通过给部分分）。

> **三次月考（10 月 / 11 月 / 12 月初）与期末上机考试是同一规格**：
> **6 题 / 112 分钟**。
> 第 5 周与第 14 周的样卷就是按这个规格出的——**月考不是缩水版，是同构演练**。

**成绩参考**（历年经验，最终以当学期教学通知为准）：

- **AC 5–6 题**：基本可获优秀；
- **AC 4 题**：若笔试成绩较高且未超优秀比例限制，仍有机会评优；
- **AC 0 题**：即使笔试满分，总评也会受到明显影响。

> ⚠️ 上述比例与条件**不是本讲义能确定的**，仅用于说明机考的重要性。
> **一切以当学期正式教学通知为准。**

---

# 3 能力要求

本次考试考查以下五项能力，命题时每道题至少覆盖其中两项：

1. **问题抽象**：把文字描述翻译成数据结构与状态模型——**这是最常见的失分点**；
2. **算法选择**：根据数据范围选出复杂度可行的算法（第 4 周 3.4 节的表）；
3. **代码实现**：在**无 AI 辅助**下正确、快速地写出可运行代码；
4. **边界处理**：n = 0/1、多组数据、全相同、极值、不连通、空输入；
5. **调试能力**：读懂 WA / TLE 反馈并自行构造数据定位。

---

# 4 知识点覆盖矩阵（教师用）

命题时保证 6 道题覆盖以下**至少 5 个大类**，且**贪心、DP、搜索各至少 1 题**：

| 大类 | 周次 | 本卷覆盖 | 具体考点 |
| ---- | ---- | ---- | ---- |
| 语法与容器 | W1–W2、W4 | T1 | 字典计数、多关键字排序、格式化输出 |
| 计算机基础 | W3、W13 | T1 备选 | 进制、位运算、ASCII、复杂度估算 |
| 线性结构 | W7 | T2 | 栈 / 队列 / 单调栈 |
| 排序与贪心 | W6、W10 | T3 | 排序键、区间问题、差分 |
| 搜索 | W9、W12 | T4 | BFS / DFS / 回溯 / 带状态搜索 |
| 动态规划 | W10–W11 | T5 | 线性 DP / 背包 |
| 并查集与图 | W9、W12 | T6 | 连通性、最小瓶颈路、二分答案 |
| 矩阵 | W6 | 备选 | 二维遍历、前缀和、卷积 |

**难度梯度**：

```
T1  ★☆☆☆☆   签到题，90% 学生应 AC        —— 保证及格线
T2  ★★☆☆☆   基础数据结构应用，70% AC
T3  ★★★☆☆   需要一次转化（排序键），55% AC
T4  ★★★☆☆   带状态搜索，45% AC
T5  ★★★★☆   DP，状态设计有难度，30% AC
T6  ★★★★★   综合建模，15% AC             —— 区分优秀
```

> **命题原则**：前 3 题保证认真听课、完成作业的学生能拿到 45 分；后 3 题拉开区分度。
> ⚠️ 上表的预期 AC 率是**命题经验估计**，无历史数据支撑，需按实际结果逐年校准。

---

# 5 建议样卷（一套完整试题）

---

> **T-028 更正（2026-09-22）**：下方原 T1–T6 是未标来源的草稿题，已废弃。2025-11-06 实际比赛“20251106 cs101 Mock Exam立冬前一天”的真实题目如下；完整题面、约束和提交入口以 OpenJudge 页面为准。

| 题号 | 题名 | 主要考点 | 题面 |
| --- | --- | --- | --- |
| E29982 | 一种等价类划分问题 | 哈希分组、数字各位和 | [29982](http://cs101.openjudge.cn/practice/29982/) |
| E30086 | dance | 排序、贪心配对 | [30086](http://cs101.openjudge.cn/practice/30086/) |
| M25570 | 洋葱 | 矩阵分层 / 模拟 | [25570](http://cs101.openjudge.cn/practice/25570/) |
| M28906 | 数的划分 | DFS / 动态规划 | [28906](http://cs101.openjudge.cn/practice/28906/) |
| M29896 | 购物 | 贪心、覆盖连续金额 | [29896](http://cs101.openjudge.cn/practice/29896/) |
| T25353 | 排队 | 可交换关系、贪心 / 数据结构 | [25353](http://cs101.openjudge.cn/practice/25353/) |

以下旧 T1–T6 内容仅保留作历史审计，不再作为真题讲解。

## T1. E29982 一种等价类划分问题

**题意复述**：在 `(m,n)` 内筛出各位数字和是 `k` 的倍数的整数，再按“各位数字和”分组；每组升序、逗号分隔，组按数字和递增输出。输入格式为 `m,n,k`。

```python
m, n, k = map(int, input().split(','))
groups = {}
for x in range(m + 1, n):
    s = sum(map(int, str(x)))
    if s % k == 0:
        groups.setdefault(s, []).append(str(x))
for s in sorted(groups):
    print(','.join(groups[s]))
```

题目：[E29982 一种等价类划分问题](http://cs101.openjudge.cn/practice/29982/)。

## T2. E30086 dance

**题意复述**：有 `2N` 名学生，要求两两配对且每对身高差不超过 `D`。将身高排序后只能相邻配对；所有相邻差均不超过 `D` 时输出 `Yes`，否则 `No`。

```python
n, d = map(int, input().split())
a = sorted(map(int, input().split()))
print('Yes' if all(a[i + 1] - a[i] <= d for i in range(0, 2 * n, 2)) else 'No')
```

题目：[E30086 dance](http://cs101.openjudge.cn/practice/30086/)。

## T3. M25570 洋葱

**题意复述**：给定 `n*n` 非负矩阵，逐层剥去外圈，求所有层元素和的最大值。每层是方框边界；奇数阶中心元素单独成层。

```python
n = int(input())
a = [list(map(int, input().split())) for _ in range(n)]
best = 0
for layer in range((n + 1) // 2):
    lo, hi = layer, n - 1 - layer
    total = sum(a[lo][j] for j in range(lo, hi + 1))
    if hi > lo:
        total += sum(a[hi][j] for j in range(lo, hi + 1))
        total += sum(a[i][lo] + a[i][hi] for i in range(lo + 1, hi))
    best = max(best, total)
print(best)
```

题目：[M25570 洋葱](http://cs101.openjudge.cn/practice/25570/)。

## T4. M28906 数的划分

**题意复述**：把 `n` 分成 `k` 个非空正整数，顺序不计，求方案数（`n <= 200, 2 <= k <= 6`）。令下一份不小于上一份即可避免排列重复。

```python
from functools import lru_cache
n, k = map(int, input().split())
@lru_cache(None)
def dfs(rem, left, low):
    if left == 1:
        return int(rem >= low)
    return sum(dfs(rem - x, left - 1, x)
               for x in range(low, rem // left + 1))
print(dfs(n, k, 1))
```

`low` 保证后续各份不小于当前份，因此同一划分不会因排列重复计数。

题目：[M28906 数的划分](http://cs101.openjudge.cn/practice/28906/)。

## T5. M29896 购物

**题意复述**：有无限枚不同面值硬币，求最少带多少枚硬币，使 `1..X` 每个金额都能组合出来；不能覆盖时输出 `-1`。维护当前连续可覆盖区间 `[1, reach]`，每次选不超过 `reach+1` 的最大面值。

```python
X, n = map(int, input().split())
coins = sorted(map(int, input().split()))
reach = count = 0
while reach < X:
    usable = [c for c in coins if c <= reach + 1]
    if not usable:
        print(-1); break
    reach += max(usable); count += 1
else:
    print(count)
```

题目：[M29896 购物](http://cs101.openjudge.cn/practice/29896/)。

## T6. T25353 排队

**题意复述**：相邻两人身高差不超过 `D` 才能交换，任意次交换后求字典序最小的身高序列。两人身高差大于 `D` 时相对顺序永远不能改变，可把它们看成前驱约束；每个元素的层数是此前所有“不可交换的大山”的最大层数加一，同层元素可任意交换，故逐层排序输出。

**参考解答**：离散化身高后，用两棵树状数组维护“比当前值小 `D` 以上”和“比当前值大 `D` 以上”的最大层数，单点更新、前缀最大查询均为 `O(log N)`。

```python
import bisect
import sys

data = list(map(int, sys.stdin.buffer.read().split()))
n, d = data[:2]
h = data[2:2 + n]
vals = sorted(set(h))
m = len(vals)
lo_bit = [0] * (m + 1)
hi_bit = [0] * (m + 1)

def update(bit, i, value):
    while i <= m:
        bit[i] = max(bit[i], value)
        i += i & -i

def query(bit, i):
    ans = 0
    while i:
        ans = max(ans, bit[i])
        i -= i & -i
    return ans

layers = {}
for height in h:
    small = query(lo_bit, bisect.bisect_left(vals, height - d))
    large = query(hi_bit, m - bisect.bisect_right(vals, height + d))
    level = max(small, large) + 1
    layers.setdefault(level, []).append(height)
    pos = bisect.bisect_left(vals, height) + 1
    update(lo_bit, pos, level)
    update(hi_bit, m - pos + 1, level)
for level in sorted(layers):
    for height in sorted(layers[level]):
        print(height)
```

样例 `7 7 3 6 2, D=3` 的结果为 `6 7 7 2 3`；不能直接排序成 `2 3 6 7 7`，因为 `2` 无法越过身高差超过 `D` 的人。

题目：[T25353 排队](http://cs101.openjudge.cn/practice/25353/)。

<!-- 历史草稿原文保留在本文件此处，仅供审计，不属于当前样卷正文。

**考点**：字典计数、多关键字排序、格式化输出（W2、W4）　　**难度**：★☆☆☆☆

### 题面

有 n 条成绩记录，每条包含**学号**（7 位数字字符串）、**科目名**（小写字母，长度 ≤ 10）和**分数**（0–100 的整数）。

请按**总分从高到低**输出每个学号的总分与平均分（保留两位小数）；**总分相同时按学号字典序从小到大**。

**输入**
第一行一个整数 n（1 ≤ n ≤ 2×10⁵）。
接下来 n 行，每行 `学号 科目 分数`，以空格分隔。

**输出**
每个学号一行：`学号 总分 平均分`（平均分保留两位小数）。

**样例输入**
```
5
2200011 math 90
2200011 physics 80
2200012 math 95
2200012 physics 75
2200013 math 85
```

**样例输出**
```
2200011 170 85.00
2200012 170 85.00
2200013 85 85.00
```

**样例解释**：2200011 与 2200012 总分同为 170，按学号升序排列。

### 参考解答

```python
import sys
from collections import defaultdict


def solve():
    data = sys.stdin.read().split()
    n = int(data[0])
    total = defaultdict(int)
    cnt = defaultdict(int)
    idx = 1
    for _ in range(n):
        sid, _subject, score = data[idx], data[idx + 1], int(data[idx + 2])
        idx += 3
        total[sid] += score
        cnt[sid] += 1
    order = sorted(total, key=lambda s: (-total[s], s))
    out = [f"{s} {total[s]} {total[s] / cnt[s]:.2f}" for s in order]
    sys.stdout.write('\n'.join(out) + '\n')


solve()
```

### 数据构造建议

- **卡 O(n²)**：n = 2×10⁵ 条记录但只有 500 个不同学号 —— 用 `list.count()` 或
  每次线性查找会稳定 TLE。
- **卡排序键**：构造大量总分相同的学号，检验并列时是否按**学号升序**。
- **卡格式**：平均分恰好为整数（如 85）时必须输出 `85.00`，不能是 `85.0` 或 `85`。
- **卡输入规模**：用 `input()` 逐行读 2×10⁵ 行会明显变慢，检验是否用了快速输入。

### 评分标准

| 项 | 分值 |
| ---- | ---- |
| 用字典一遍统计（而非重复扫描） | 6 |
| 多关键字排序 `(-total, sid)` 正确 | 5 |
| 平均分格式 `:.2f` 正确 | 4 |

---

### 历史草稿 T2：括号嵌套深度

**考点**：栈、边界判断（W7）　　**难度**：★★☆☆☆

### 题面

给定一个字符串 s，其中可能包含 `(` `)` `[` `]` `{` `}` 以及其他任意可见字符。

- 若括号**完全匹配**，输出 `YES` 和**最大嵌套深度**（用空格分隔）；
- 否则输出 `NO` 和**第一个出错位置**（1-indexed）。

"出错位置"定义为：
1. 遇到一个右括号，但栈为空或栈顶与之不配对 → 该右括号的位置；
2. 扫描结束时仍有未闭合的左括号 → 位置为 `len(s) + 1`。

**输入**：一行字符串 s（0 ≤ |s| ≤ 10⁶，不含空格）。

**输出**：一行，格式如上。

**样例输入**
```
a(b[c]{d})e
```

**样例输出**
```
YES 2
```

**更多样例**

| 输入 | 输出 | 说明 |
| ---- | ---- | ---- |
| `(]` | `NO 2` | 第 2 位的 `]` 与栈顶 `(` 不配对 |
| `((()` | `NO 5` | 扫描结束仍有 2 个 `(` 未闭合，输出 `4+1=5` |
| `()[]{}` | `YES 1` | 三对括号并列，最大深度为 1 |
| `(([]))` | `YES 3` | 最深处是 `[]`，嵌套 3 层 |

### 参考解答

```python
import sys


def solve():
    s = sys.stdin.readline().rstrip('\n')
    pairs = {')': '(', ']': '[', '}': '{'}
    stack, depth, best = [], 0, 0
    for i, ch in enumerate(s):
        if ch in '([{':
            stack.append(ch)
            depth += 1
            if depth > best:
                best = depth
        elif ch in pairs:
            if not stack or stack[-1] != pairs[ch]:
                print("NO", i + 1)
                return
            stack.pop()
            depth -= 1
    if stack:
        print("NO", len(s) + 1)
    else:
        print("YES", best)


solve()
```

### 数据构造建议

- **空串**：输出应为 `YES 0`。
- **只有非括号字符**：`abcdef` → `YES 0`。
- **深嵌套**：10⁵ 层 `((((...))))` —— 检验是否用了递归（会爆栈）而非栈。
- **先深后错**：`((([))` —— 检验是否在报错前误输出了深度。
- **末尾未闭合**：`(((` → `NO 4`，检验 `len(s)+1` 这个边界。

### 评分标准

| 项 | 分值 |
| ---- | ---- |
| 栈匹配逻辑正确（三类错误全覆盖） | 8 |
| 最大深度统计正确 | 4 |
| 未闭合时输出 `len(s)+1` | 3 |

---

### 历史草稿 T3：会议室数量

**考点**：区间分组、差分 / 排序（W10）　　**难度**：★★★☆☆

### 题面

有 n 场会议，第 i 场的时间是**左闭右开**区间 [aᵢ, bᵢ)（即 bᵢ 时刻可以立刻安排下一场）。
求最少需要多少间会议室，使所有会议都能进行。

**输入**
第一行整数 n（1 ≤ n ≤ 2×10⁵）。
接下来 n 行，每行两个整数 aᵢ bᵢ（0 ≤ aᵢ < bᵢ ≤ 10⁹）。

**输出**：一个整数。

**样例输入**
```
3
0 30
5 10
15 20
```

**样例输出**
```
2
```

**样例解释**：[5,10) 与 [15,20) 可共用一间（前者结束后者才开始），[0,30) 独占一间。

### 参考解答

**思路**：答案 = **同一时刻最多有多少场会议在进行**。
把每场会议拆成"+1（开始）"和"−1（结束）"两个事件，按时间排序后扫描求峰值。

**关键细节**：左闭右开时，同一时刻的 `−1` 必须排在 `+1` **前面**（先释放再占用）。
Python 中 `(t, -1) < (t, 1)` 自动满足这一点。

```python
import sys


def solve():
    data = sys.stdin.buffer.read().split()
    n = int(data[0])
    events = []
    idx = 1
    for _ in range(n):
        a, b = int(data[idx]), int(data[idx + 1])
        idx += 2
        events.append((a, 1))
        events.append((b, -1))
    events.sort()                      # (t, -1) 排在 (t, 1) 前面
    cur = best = 0
    for _, delta in events:
        cur += delta
        if cur > best:
            best = cur
    print(best)


solve()
```

**堆写法**（等价，便于理解"分组"的含义）：

```python
import heapq


def min_rooms(intervals):
    intervals = sorted(intervals, key=lambda x: x[0])
    heap = []                           # 各会议室当前的结束时间
    for a, b in intervals:
        if heap and heap[0] <= a:       # 最早结束的那间已空出（左闭右开取 <=）
            heapq.heapreplace(heap, b)
        else:
            heapq.heappush(heap, b)
    return len(heap)


print(min_rooms([(0, 30), (5, 10), (15, 20)]))     # 2
print(min_rooms([(1, 4), (2, 5), (6, 8), (3, 7)])) # 3
```

### 数据构造建议

- **端点相接**：大量 `[0,1) [1,2) [2,3) ...` —— 若把 `−1` 排在 `+1` 之后，会错答成 2。
- **全嵌套**：`[0,n) [1,n-1) [2,n-2) ...` —— 答案应等于 n。
- **坐标范围 10⁹**：检验是否试图开长度为坐标范围的数组（会 MLE）。
- **n = 2×10⁵**：检验是否用了 O(n²) 的两两比较。

### 评分标准

| 项 | 分值 |
| ---- | ---- |
| 想到"求同时进行的峰值"这一转化 | 7 |
| 事件排序 / 堆写法实现正确 | 5 |
| 左闭右开的边界处理正确 | 3 |

---

### 历史草稿 T4：穿墙迷宫

**考点**：带状态的 BFS（W12）　　**难度**：★★★☆☆

### 题面

n×m 的网格，`.` 表示空地、`#` 表示墙、`S` 是起点、`T` 是终点（`S` 与 `T` 都是空地）。
每一步可以走到上下左右相邻的格子；**最多可以穿过 k 面墙**（穿墙同样计 1 步）。
求从 `S` 到 `T` 的最少步数。

**输入**
第一行三个整数 n m k（1 ≤ n, m ≤ 500，0 ≤ k ≤ 10）。
接下来 n 行，每行 m 个字符。

**输出**：最少步数；若无法到达（即使用满 k 次穿墙也不行）输出 `-1`。

**样例输入**
```
3 5 2
S#.#T
.#.#.
.....
```

**样例输出**
```
4
```

**样例解释**：穿过 (1,2) 与 (1,4) 两面墙，走 `S → # → . → # → T`，共 4 步。
若 k = 0 或 k = 1，则必须绕行，答案均为 8。

### 参考解答

**关键**：状态是 **`(x, y, 已穿墙次数)`** —— 把网格复制成 k+1 层。

```python
import sys
from collections import deque


def solve():
    data = sys.stdin.read().split()
    n, m, k = int(data[0]), int(data[1]), int(data[2])
    grid = data[3:3 + n]
    sx = sy = tx = ty = -1
    for i in range(n):
        row = grid[i]
        for j in range(m):
            if row[j] == 'S':
                sx, sy = i, j
            elif row[j] == 'T':
                tx, ty = i, j
    DIRS = ((-1, 0), (1, 0), (0, -1), (0, 1))
    # dist[used][x][y]
    dist = [[[-1] * m for _ in range(n)] for _ in range(k + 1)]
    dist[0][sx][sy] = 0
    q = deque([(sx, sy, 0)])
    while q:
        x, y, u = q.popleft()
        if (x, y) == (tx, ty):
            print(dist[u][x][y])
            return
        for dx, dy in DIRS:
            nx, ny = x + dx, y + dy
            if not (0 <= nx < n and 0 <= ny < m):
                continue
            nu = u + (1 if grid[nx][ny] == '#' else 0)
            if nu > k or dist[nu][nx][ny] >= 0:
                continue
            dist[nu][nx][ny] = dist[u][x][y] + 1
            q.append((nx, ny, nu))
    print(-1)


solve()
```

**为什么普通 BFS 是对的（不需要 Dijkstra）**：每一步的代价都是 1（穿墙也是 1 步），
边权相同，所以 BFS 第一次到达 `T` 就是最短。**若穿墙代价不是 1，就必须换成 Dijkstra。**

### 数据构造建议

- **k = 0 且被墙完全隔开** → `-1`；
- **k 足够大** → 答案退化为曼哈顿距离；
- **500×500 且 k = 10** → 状态数 2.75×10⁶，检验是否用 `deque`（用 `list.pop(0)` 必 TLE）；
- **只用 `visited[x][y]` 一层**的错解：在样例上就会答成 8 而非 4 —— 这是最典型的错法，
  样例必须能卡住它；
- **S 与 T 相邻且中间是墙**：`S#T`，k=0 → −1，k=1 → 2。

### 评分标准

| 项 | 分值 |
| ---- | ---- |
| 状态加"已穿墙次数"这一维 | 7 |
| BFS 实现正确（deque + 入队标记） | 5 |
| 边界（不可达、k=0、S/T 相邻）正确 | 3 |

---

### 历史草稿 T5：作业时间分配

**考点**：0-1 背包 + 次要目标（W11）　　**难度**：★★★★☆

### 题面

期末周还剩 T 分钟。有 n 份作业，第 i 份需要 tᵢ 分钟，完成可得 sᵢ 分。
每份作业**要么完整完成、要么不做**。

求在总时间不超过 T 的前提下能拿到的**最高总分**；
若有多种方案能拿到最高分，再求其中**总耗时最少**的那个耗时。

**输入**
第一行两个整数 n T（1 ≤ n ≤ 100，1 ≤ T ≤ 10⁴）。
接下来 n 行，每行两个整数 tᵢ sᵢ（1 ≤ tᵢ ≤ T，1 ≤ sᵢ ≤ 10⁴）。

**输出**：一行两个整数——最高总分、达到该总分的最小耗时。

**样例输入**
```
4 10
3 10
4 14
5 16
2 5
```

**样例输出**
```
31 10
```

**样例解释**：选第 1、2、4 份（3+4+2 = 9 分钟，10+14+5 = 29 分）不是最优；
选第 1、3、4 份（3+5+2 = 10 分钟，10+16+5 = 31 分）为最优，耗时 10 分钟。

### 参考解答

**双目标 DP**：主目标最大化得分，次目标最小化耗时。
维护两个数组：`dp[c]` = 容量 c 下的最高分，`tm[c]` = 达到该分数的最小耗时。

```python
import sys


def solve():
    data = sys.stdin.read().split()
    n, T = int(data[0]), int(data[1])
    dp = [0] * (T + 1)          # 最高得分
    tm = [0] * (T + 1)          # 达到 dp[c] 的最小耗时
    idx = 2
    for _ in range(n):
        t, s = int(data[idx]), int(data[idx + 1])
        idx += 2
        for c in range(T, t - 1, -1):        # 0-1 背包：倒序
            cand = dp[c - t] + s
            if cand > dp[c] or (cand == dp[c] and tm[c - t] + t < tm[c]):
                dp[c] = cand
                tm[c] = tm[c - t] + t
    best = max(dp)
    min_time = min(tm[c] for c in range(T + 1) if dp[c] == best)
    print(best, min_time)


solve()
```

> **两个易错点**：
> 1. **必须倒序**（0-1 背包），正序会让同一份作业被做多次；
> 2. 两个分支缺一不可：`cand > dp[c]`（**分更高就必须覆盖**）
>    与 `cand == dp[c] and tm[c-t] + t < tm[c]`（**分相同时才比耗时**）。
>    只写后一支（`if cand >= dp[c] and tm[c-t] + t < tm[c]`）会漏掉"分更高"的情况，
>    样例直接输出 `0 0`。
>    （注意：把第二支里的 `==` 单独换成 `>=` 反而**与原式等价** ——
>    因为 `cand > dp[c]` 那一支已经在 `or` 的左边了。错的是**少一支**，不是这个符号。）

### 数据构造建议

- **多方案同分**：构造两组作业得分相同但耗时不同（如 `(3,10)` `(5,10)`），检验次要目标；
- **T = 10⁴、n = 100**：10⁶ 次转移，检验是否写成了 O(n·T·n) 的三重循环；
- **所有 tᵢ > T**：答案应为 `0 0`；
- **单份作业恰好用满 T**：检验倒序边界 `range(T, t-1, -1)`；
- **暴力对拍**：n ≤ 15 时用 2ⁿ 枚举子集，验证 (得分, 耗时) 两个目标都对。

### 评分标准

| 项 | 分值 |
| ---- | ---- |
| 0-1 背包主目标正确（含倒序） | 10 |
| 次要目标"同分取最小耗时"正确 | 7 |
| 边界（无解、恰好用满）正确 | 3 |

---

### 历史草稿 T6：电网巡检

**考点**：最小瓶颈路 = 排序 + 并查集（W6、W9）；亦可二分 + BFS（W12）　　**难度**：★★★★★

### 题面

电网有 n 个变电站（编号 1..n）和 m 条双向线路，第 j 条连接 uⱼ 与 vⱼ，**危险度**为 wⱼ。

巡检队要从 1 号站走到 n 号站。一条路径的"风险"定义为**该路径上所有线路危险度的最大值**。
求所有路径中风险的**最小值**。

**输入**
第一行两个整数 n m（2 ≤ n ≤ 10⁵，1 ≤ m ≤ 2×10⁵）。
接下来 m 行，每行三个整数 uⱼ vⱼ wⱼ（1 ≤ uⱼ, vⱼ ≤ n，uⱼ ≠ vⱼ，1 ≤ wⱼ ≤ 10⁹）。

**输出**：最小风险值；若 1 与 n 不连通，输出 `-1`。

**样例输入**
```
4 4
1 2 5
2 3 3
3 4 7
1 4 6
```

**样例输出**
```
6
```

**样例解释**：路径 1→4 的风险是 6；路径 1→2→3→4 的风险是 max(5,3,7) = 7。取较小者 6。

### 参考解答

**核心洞察**：**按危险度从小到大逐条加入线路**，第一次让 1 与 n 连通的那条线路的危险度，
就是答案。因为再小的危险度上限都无法连通 1 与 n。

```python
import sys


def solve():
    data = sys.stdin.buffer.read().split()
    n, m = int(data[0]), int(data[1])
    edges = []
    idx = 2
    for _ in range(m):
        u, v, w = int(data[idx]), int(data[idx + 1]), int(data[idx + 2])
        idx += 3
        edges.append((w, u, v))
    edges.sort()                              # 按危险度升序

    parent = list(range(n + 1))
    size = [1] * (n + 1)

    def find(x):
        root = x
        while parent[root] != root:
            root = parent[root]
        while parent[x] != root:              # 路径压缩（迭代，防爆栈）
            parent[x], x = root, parent[x]
        return root

    for w, u, v in edges:
        ru, rv = find(u), find(v)
        if ru != rv:
            if size[ru] < size[rv]:
                ru, rv = rv, ru
            parent[rv] = ru
            size[ru] += size[rv]
        if find(1) == find(n):
            print(w)
            return
    print(-1)


solve()
```

**另一种解法：二分答案 + BFS**（O((n+m) log W)，思路更直观但更慢）：

```python
from collections import defaultdict, deque


def min_risk_binary(n, edges):
    values = sorted({w for w, _, _ in edges})

    def connected(limit):
        adj = defaultdict(list)
        for w, u, v in edges:
            if w <= limit:
                adj[u].append(v)
                adj[v].append(u)
        seen = {1}
        q = deque([1])
        while q:
            x = q.popleft()
            for y in adj[x]:
                if y not in seen:
                    seen.add(y)
                    q.append(y)
        return n in seen

    lo, hi = 0, len(values) - 1
    if not connected(values[hi]):
        return -1
    while lo < hi:
        mid = (lo + hi) // 2
        if connected(values[mid]):
            hi = mid
        else:
            lo = mid + 1
    return values[lo]


print(min_risk_binary(4, [(5, 1, 2), (3, 2, 3), (7, 3, 4), (6, 1, 4)]))   # 6
print(min_risk_binary(3, [(1, 1, 2)]))                                    # -1
```

### 数据构造建议

- **不连通**：n = 3、只有边 (1,2) → `-1`；
- **重边**：同一对点多条不同危险度的边，检验是否取到最小的那条；
- **链状图**：1–2–3–…–n，检验并查集是否退化（未做路径压缩会 TLE）；
- **n = 10⁵、m = 2×10⁵**：检验是否用了快速输入与 `sort`；
- **全部边权相同**：答案就是该权值，检验"第一次连通即返回"的逻辑；
- **交叉验证**：用二分 + BFS 的实现与并查集实现在随机小图上对拍（本题已做 400 组）。

### 评分标准

| 项 | 分值 |
| ---- | ---- |
| 想到"按边权升序加边，首次连通即答案"（或等价的二分判定） | 10 |
| 并查集实现正确（路径压缩 + 按大小合并） | 6 |
| 不连通输出 `-1`、重边处理正确 | 4 |

---

## 5.7 红队测试数据（已实际验证）

以下数据已由 [`tools/redteam_exam.py`](../tools/redteam_exam.py) 对照正确解与错误实现实际运行。
它们不是"可能有用"的出题建议：每项都能使标明的错误解 WA、TLE、MLE 或触发递归深度错误。

| 题 | 固定反例 / 大数据形状 | 正确结果 | 已证实能卡住的错误实现 |
| ---- | ---- | ---- | ---- |
| T1 | `2200002 math 85`、`2200001 math 85`；另 8000 个不同学号 | 学号升序、`85.00` | 保留输入顺序 / `str(float)` WA；`sid in list` 实测至少慢 20 倍，按规模外推不可用 |
| T2 | `(]`；另 `(` 重复超过 Python 递归深度 | `NO 2` | 只计数不验类型 WA；递归扫描 `RecursionError` |
| T3 | `[0,1)`、`[1,2)`；另 `[0,10^9)` | 1 间 | 同时刻先开始后结束 WA（输出 2）；按坐标开数组 MLE |
| T4 | `5 5 1 / S#.#. / ...#. / #.... / .#### / #.#.T` | 8 | 只维护 `visited[x][y]` 一层 WA（输出 -1） |
| T5 | `1 10 / 3 5`；`2 5 / 5 10 / 3 10` | `5 3`；`10 3` | 正序背包重复选同一作业 WA；同分时取耗时较大方案 WA |
| T6 | `3 3 / 1 2 5 / 2 3 5 / 1 3 6`；另 10 万点链 | 5 | 按路径总和而非瓶颈值 WA（输出 6）；未路径压缩链构造有超过 40 亿次父指针遍历 |

运行：

```bash
python3 tools/redteam_exam.py
```

---

-->

## 5.7 当前样卷红队反例

[`tools/redteam_exam.py`](../tools/redteam_exam.py) 对当前六道真题的正确模型与代表性错误实现实际运行。以下不是评分点，也不替代平台数据；它们只保证常见错法会在固定合法输入上暴露。

| 题 | 固定反例 | 能卡住的错误实现 |
| --- | --- | --- |
| T1 | `(11,35,3)` | 把所有“各位和为 3 的倍数”的数混成一组 |
| T2 | 身高 `3,100,4,101`，`D=2` | 不排序、只配原相邻位置 |
| T3 | 3×3 矩阵中心为 100、其余为 1 | 外层只累加上下两行，漏两侧 |
| T4 | `n=7,k=3` | 按有序拆分计数（15 而非 4） |
| T5 | `X=20`，面值 `1,2,5,10` | 每次拿最小可用面值（20 而非 5 枚） |
| T6 | `7,7,3,6,2`，`D=3` | 直接全局排序，越过不可交换屏障 |

运行：

```bash
python3 tools/redteam_exam.py
```

# 6 备选题库（按知识点分类）

可用于替换样卷中的任意一题，或作为往年真题复习使用。

## 6.1 语法、字符串与容器（W1–W4）

| 题目 | 编号 | 考点 |
| ---- | ---- | ---- |
| 鸡兔同笼 | E02750 | 分支、整除 |
| 大小写字母互换 | E02689 | ASCII |
| 整数的个数 | E02676 | 计数 |
| 生日相同 | E02724 | 字典分组 + 排序 |
| 词典 | E02804 | 字典查询 |
| 多项式时间复杂度 | E23563 | 字符串解析 |
| 文字排版 | E06374 | 模拟 |
| 验证"歌德巴赫猜想" | E03143 | 素数筛 |
| 十进制到八进制 | E02734 | 进制 |

## 6.2 矩阵（W6）

| 题目 | 编号 | 考点 |
| ---- | ---- | ---- |
| 矩阵运算（先乘再加） | E18161 | 矩阵乘法 + 维度判断 |
| 计算矩阵边缘元素之和 | E07743 | 二维遍历 |
| 矩阵交换行 | 02899 | 二维列表 |
| 二维矩阵上的卷积运算 | E19942 | 保护圈 |
| 节省存储的矩阵乘法 | E23555 | 稀疏矩阵 |
| 螺旋矩阵 | M18106 | 方向数组 |
| 图的拉普拉斯矩阵 | E19943 | 图 + 矩阵 |

## 6.3 排序、贪心与区间（W6、W10）

| 题目 | 编号 | 考点 |
| ---- | ---- | ---- |
| 装箱问题 | 01017 | 贪心 + 打表 |
| 最大最小整数 | 12559 | 自定义比较 |
| 因材施教 | 19948 | 排序后切间隙 |
| 军备竞赛 | 18211 | 双指针贪心 |
| 排队做实验 | M21554 | 排序 |
| 买学区房 | M19963 | 排序 + 中位数 |
| 校门外的树 | 02808 | 差分 |
| 校门外的树又来了 | M29947 | 合并区间 |
| Radar Installation | M01328 | 区间选点 |
| 世界杯只因 | T27104 | 区间覆盖 |

## 6.4 栈与队列（W7）

| 题目 | 编号 | 考点 |
| ---- | ---- | ---- |
| 波兰表达式 | 02694 | 前缀求值 |
| 后序表达式求值 | 24588 | 后缀求值 |
| 快速堆猪 | 22067 | 辅助栈 |
| 双端队列 | 05902 | deque |
| 约瑟夫问题 | 02746 | 队列模拟 |
| 病人排队 | E07618 | 稳定排序 |
| 有多少种合法的出栈顺序 | 27217 | 卡特兰数 |
| 土豪购物 | M20744 | 线性 DP |

## 6.5 递归、回溯与并查集（W8–W9）

| 题目 | 编号 | 考点 |
| ---- | ---- | ---- |
| 菲波那契数列 | 02753 | 递归 + 记忆化 |
| Pell 数列 | M02786 | 递推 |
| 汉诺塔问题(Tower of Hanoi) | 04147 | 递归三步 |
| 全排列 | 02748 | 回溯模板 |
| 八皇后 | 02754 | 回溯 + 剪枝 |
| 马走日 | 04123 | 回溯 + 状态还原 |
| Lake Counting | 02386 | Flood Fill |
| 晶矿的个数 | M05585 | 连通块 |
| 宗教信仰 | 02524 | 并查集 |
| 一种等价类划分问题 | M29982 | 并查集 |
| 食物链 | T01182 | 扩展域并查集 |
| 放苹果 | 01664 | 递归计数 |
| 简单的整数划分问题 | 04117 | 递归 + 记忆化 |

## 6.6 动态规划（W10–W11）

| 题目 | 编号 | 考点 |
| ---- | ---- | ---- |
| 数字三角形 | 02760 | 路径 DP |
| 采药 | 02773 | 0-1 背包 |
| 小偷背包 | 23421 | 0-1 背包 |
| Coins | M01742 | 多重背包 |
| 宠物小精灵之收服 | 04102 | 二维费用背包 |
| 健身房 (dp) | 21458 | "恰好装满" |
| NBA 门票 | 20089 | "恰好 + 最少个数" |
| Longest Ordered Subsequence | 02533 | LIS |
| 拦截导弹 | M02945 | 最长不上升子序列 |
| 最大上升子序列和 | 03532 | LIS 变形 |
| 公共子序列 | 02806 | LCS |
| 最大子矩阵 | M02766 | 降维 + Kadane |
| 合唱队形 | 02711 | 双向 LIS |

## 6.7 搜索与二分（W12）

| 题目 | 编号 | 考点 |
| ---- | ---- | ---- |
| 鸣人和佐助 | 04115 | 带状态 BFS |
| 拯救行动 | 04116 | BFS + 优先队列 |
| 水淹七军 | M12029 | BFS 模拟 |
| 寻宝 | 19930 | BFS |
| 变换的迷宫 | T04129 | 多维状态 BFS |
| 小游戏 | T02802 | BFS + 转弯 |
| 走山路 | M20106 | Dijkstra |
| 河中跳房子 | M08210 | 二分答案 |
| 月度开销 | M04135 | 二分答案 |
| Aggressive cows | M02456 | 二分答案 |
| Expedition | M02431 | 贪心 + 堆 |

---

# 7 给学生的备考建议

## 7.1 必须能默写的 12 个模板

| # | 模板 | 周次 | 自测 |
| - | ---- | ---- | ---- |
| 1 | 快速输入 `sys.stdin.read().split()` | W4 | ☐ |
| 2 | 埃氏筛 | W4 | ☐ |
| 3 | 一维 / 二维前缀和 + 差分 | W6、W10 | ☐ |
| 4 | 多关键字排序 `key=lambda x: (a, -b)` | W6 | ☐ |
| 5 | 归并排序的合并（求逆序对） | W6 | ☐ |
| 6 | 单调栈（下一个更大元素） | W7 | ☐ |
| 7 | 回溯模板（选择 → 递归 → 撤销） | W9 | ☐ |
| 8 | 并查集（路径压缩 + 按大小合并） | W9 | ☐ |
| 9 | 0-1 背包（倒序）/ 完全背包（正序） | W11 | ☐ |
| 10 | LIS 的 O(n log n) 写法 | W11 | ☐ |
| 11 | BFS（deque + 入队标记 + 状态维） | W12 | ☐ |
| 12 | 二分答案（判定 + 上 / 下取整） | W12 | ☐ |

**自测方法**：关掉所有资料，在空文件里默写。写不出来的，回到对应周的讲义重看一遍再默写。

## 7.2 一页 A4 cheat sheet 的建议内容

**正面**：

1. 快速输入 / 输出模板（3 行）
2. 复杂度速查表（n 的范围 → 可用复杂度）
3. 容器操作复杂度表
4. 多关键字排序、`bisect` 用法
5. 前缀和 / 差分公式
6. 二分模板（两种取整方向）

**反面**：

7. 回溯模板骨架
8. 并查集（10 行）
9. 0-1 / 完全背包的两行核心循环 + "恰好装满"的初始化
10. BFS 模板（含状态维）
11. Dijkstra 模板（heapq）
12. 高频陷阱清单（第 7.4 节）

> ⚠️ **手写、双面、一页**。不得打印，不得整页抄代码。
> **写 cheat sheet 的过程本身就是最有效的复习**——抄不下的内容，说明你还没消化。

## 7.3 考场流程

```
   0–5 min     通读 6 题，按预估难度标序，先做有把握的
   5–15 min    T1（签到）—— 写完就交，用 OJ 反馈代替自己检查
   15–45 min   T2、T3
   45–78 min   T4、T5
   78–102 min  T6 或回头补前面卡住的题
   102–112 min 检查输出格式：多余空格、换行、精度、特殊边界
```

**六条纪律**：

1. **先看数据范围再想算法**（第 4 周 3.4 节）；
2. **样例过了先交**，OJ 的反馈比自己盯屏幕快；
3. **WA 就造数据**：n=1、全相同、最大值、最小值、不连通；
4. **TLE 先查复杂度**，不要急着抠常数；
5. **卡满 15 分钟就换题**，回来时往往一眼看出问题；
6. **留 10 分钟检查格式**。

## 7.4 高频陷阱清单（考前最后一遍）

- [ ] 忘 `int()` / 忘 `strip()`
- [ ] `[[0]*n]*m` 的别名陷阱
- [ ] `x in list` 是 O(n)；`list.pop(0)` 是 O(n)
- [ ] 浮点用 `==` 比较；`int(x**0.5)` 差 1
- [ ] 回溯忘 `path[:]` 拷贝 / 忘还原状态
- [ ] 0-1 背包写成正序
- [ ] "恰好装满"没用 `±inf` 初始化
- [ ] BFS 出队时才标记 visited
- [ ] 带状态的搜索少加了一维
- [ ] 二分答案的取整方向写反导致死循环
- [ ] 多关键字排序只写了一个 key
- [ ] 递归过深未 `setrecursionlimit`（或该改迭代）
- [ ] 输出格式：多余空格 / 换行 / 精度（`85.00` 而非 `85.0`）

## 7.5 三条硬性提醒

1. ⚠️ **考试禁止任何 AI 工具**，包括本地模型与 IDE 的智能补全插件。
2. ⚠️ **无法解释自己提交的代码，按学术不端处理**，成绩记 0。
3. ⚠️ 平时的自检方法：**关掉所有窗口，从空文件重写一遍**。写不出来 = 这道题你没做。

---

# 8 命题与阅卷检查清单（教师用）

## 8.1 命题前

- [ ] 6 题覆盖第 4 节矩阵中**至少 5 个大类**，且贪心 / DP / 搜索各至少 1 题；
- [ ] 难度梯度符合 ★1 → ★5，T1 是真正的签到题；
- [ ] 每题的数据范围**明确写出**，且与预期复杂度匹配；
- [ ] 每题至少给出 1 组样例，**并对易误解的输出定义给出样例解释**
      （如 T2 的"未闭合位置 = len(s)+1"、T3 的左闭右开）；
- [ ] 题面中没有歧义的边界描述（区间开闭、是否含端点、多解如何取）。

## 8.2 参考解答

- [ ] 每题的参考解答**已实际运行**并通过全部样例；
- [ ] 已测边界：n=1、空输入、全相同、极值、不连通；
- [ ] 已与暴力解**对拍**（本卷 T3 / T4 / T5 / T6 各做了 400 组随机对拍）；
- [ ] 复杂度声明与实际行为一致；
- [ ] 递归解法已确认不会在评测机上爆栈（或已改迭代）。

## 8.3 测试数据

- [ ] 每题都有**卡错解**的数据（第 5 节各题的"数据构造建议"）；
- [ ] 每题都有**卡超时**的数据，且已用错误复杂度的实现验证过它真能卡住；
- [ ] 分档数据设计合理（部分通过给部分分）；
- [ ] 时限按参考解答实测时间的 **3–5 倍**设定，并考虑 Python 与 C++ 的差异。

## 8.4 阅卷

- [ ] 评分标准逐项可核对（见各题"评分标准"表）；
- [ ] 抽查高分卷，要求学生**口头解释自己的代码**；
- [ ] 记录各题实际 AC 率，与第 4 节的预期对照，用于下一年校准。

---

# 9 小结

1. 16 周的知识体系分四条线：**计算机基础、语言与工具、算法与数据结构、AI 素养**；
   算法主线是 **枚举 → 贪心 / DP → 搜索**，横向工具是前缀和、二分、单调结构、并查集。
2. 机考 **6 题 / 112 分钟**，**禁用一切 AI 工具**，**讲不清自己的代码按学术不端处理**。
3. 样卷的六道题分别对应：**字典排序、栈、区间分组、带状态 BFS、双目标背包、最小瓶颈路**，
   难度从 ★1 递增到 ★5。
4. 备考只做两件事：**默写 12 个模板** + **手写一页 cheat sheet**。抄不下的内容说明还没消化。
5. 考场六条：看范围、早提交、造数据、查复杂度、按时换题、留时间查格式。

---

**课程到此结束。**

回头看第 1 周那句话：**这门课是练出来的**。你完成的 150–200 道题、
调过的每一个 WA、重写过的每一份代码，才是这门课真正留下的东西——
它们不会随着考试结束而消失。

下一步：《数据结构与算法》。那门课会从这里继续——树、图、哈希、字符串匹配，
以及本课只开了个头的那些主题。
